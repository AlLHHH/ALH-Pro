# ALH Pro v1.1.2 发布交接总结(给下一个开发会话)

> 写于 2026-09-03,当前 main = `3c9b307`(工作区应干净)。本文件是"发布现状 + 坑 + 下一步"的权威备忘。

## 一、发布现状(全部完成 ✅)

**GitHub Release v1.1.2**:https://github.com/AlLHHH/ALH-Pro/releases/tag/v1.1.2

| 资产 | 大小(字节) | SHA256 | 状态 |
|---|---|---|---|
| ALHPro_v1.1.2_Setup.exe | 1002559641 (956MB) | EB64E0FF3F9553545D586766D5B2BA16ADA9697F480734F5A33FC1D255D1CB3B | ✅ 已上传+下载回验一致+本机静默安装 ExitCode=0 |
| models_v1.0.zip | 1479029997 (1411MB) | 8BCC46C4348FBB2882580644114C2045DBCEB80169ABFCFFC80FC9E1781B14B2 | ✅ 同上(下载回验一致) |

**百度网盘完整版**(软件+引擎+全部模型,2.4GB,双击安装即全功能可用):
- 链接 https://pan.baidu.com/s/1_heewIWeoPpWQKPJv9blew?pwd=yxfd 提取码 `yxfd`
- 文件:`ALHPro_v1.1.2_Full_Setup.exe`(仓库根 + 桌面副本 `ALHPro_v1.1.2_完整版_Setup.exe`)
- 链接已写入:根 README、随包 README(ImgUpscalerUI\README.md)、RELEASE_NOTES.md、GitHub Release body;桌面另有 `说明.txt`(首次运行等 1~2 分钟=杀毒扫描提示,随网盘一起发)
- 本机静默安装验证:ExitCode=0,262s,主程序+7 模型+全部引擎齐全

**代码**:三轮审计 Critical 全清(磁盘预检单位、EXIF 裁剪、抠图 OOM/卡死、FfmpegSafePath、日志并发、-s 1、手动CPU被覆盖、RIFE会话/分块、DML失败标记、DML设备实测兜底等),版本 1.1.2(installer.iss / csproj / RELEASE_NOTES 一致)。

## 二、⚠️ 必须知道的坑(本次踩过的,别再犯)

1. **GitHub 大文件上传污染(最重要)**:
   - `curl -F "file=@..."` 或 .NET HttpClient multipart 上传到 `uploads.github.com` 时,文件会被污染(multipart 封装字节被一并存入,文件大 200~250B)→ 用户下载后报"**此应用无法在你的电脑上运行**"(很多人反馈,已修复)。
   - **正确姿势**:raw 上传:
     `curl.exe -sS --ssl-no-revoke -X POST -H "Authorization: token $tok" -H "Accept: application/vnd.github+json" -H "Content-Type: application/octet-stream" --data-binary "@文件" "https://uploads.github.com/repos/AlLHHH/ALH-Pro/releases/<id>/assets?name=文件名"`
   - **上传后必须下载回来核对 SHA256**(大小一致不代表一致;本机器首次上传就没核对,导致坏文件上线)。
2. **本机 curl/网络**:TLS 吊销检查被破坏 → 所有 curl 必须加 `--ssl-no-revoke`;PowerShell `Invoke-RestMethod` 对该环境(github API 小请求)可用,大文件不行。
3. **GitHub API body 更新(release notes)**:
   - `Get-Content` 默认 GBK 读 UTF-8 文件 → 乱码;**必须** `[System.IO.File]::ReadAllText(path, Encoding.UTF8)`;
   - JSON 手工构造:`{"body":"<escaped>"}`(转义 `\` `"` 与换行),UTF-8 写临时文件,curl `--data-binary @"` 发送;
   - 不要用 `Invoke-RestMethod -Body $jsonString`(PS5.1 会把中文/对象序列化坏,曾导致 release body 变成 `{"value"=>...}` 乱码)。
4. **git**:提交前看 `git status`——曾把 `备份_部署前_*` 目录误提交(1336 文件),已 `git rm -r --cached` + `.gitignore`(已加 `备份_*`)。继续警惕 `git add -A`。
5. **deploy.ps1 编码**:必须 UTF-8 **带 BOM**(一次 edit 工具改丢 BOM → PowerShell 解析失败;已恢复 BOM)。用 PowerShell 改它时用 `[IO.File]::WriteAllText(path, txt, New-Object System.Text.UTF8Encoding($true))`。
6. **iscc 编译产物**:`installer.iss`=标准包(模型不含,安装时可选下载/手动装);`installer_full.iss`=完整版(含模型,无下载任务)。两者 OutputBaseFilename 不同,AppId 相同(覆盖安装互通)。Inno Setup6 装在 `C:\Users\AlL\AppData\Local\Programs\Inno Setup 6\ISCC.exe`(用户级)。

## 三、下次发版流程(照抄)

1. 改代码 → `deploy.ps1`(发布+同步+启动验证;注意它内部输出判断别依赖 `✅ 启动正常` 文本匹配,直接看是否报错);
2. 更新 `RELEASE_NOTES.md`(软件弹窗与 release body 都读它)+ `release_history.json`(往期条目)+ 根/随包 README;
3. 重新打包:
   - `iscc installer.iss` → ALHPro_v1.1.2_Setup.exe(下一版改版本号)
   - `iscc installer_full.iss` → ALHPro_v1.1.2_Full_Setup.exe
   - 模型包:`tar -a -c -f 打包工具\models_v1.0.zip -C <含6 onnx+安装说明(必看).txt> .`(不含 RealESRGAN_x4plus.onnx!)
   - 完整版发布前 `tar -a -c -f` 打整包 zip(如需网盘 zip 版;当前网盘用 Full_Setup.exe)
4. 上传(raw 姿势):删除旧 asset → 上传 → **下载回验 SHA256**;
5. PATCH release body(正确 UTF-8 方式)+ git push;
6. 若网盘链接变了:改 README(根+随包)+RELEASE_NOTES + PATCH body(注意 release body 里也有一份)。

## 四、遗留待办(用户已知道)

- **50 系(5060/5070)最终真机确认**:代码路径+5060 三卡机早期实测已覆盖,发布后等真机反馈收尾;
- **1660Ti / AMD 独显 / Intel Arc**:代码兜底齐全,差真机样本 —— 发布后在社区按"卡型号+行/不行"收集;
- 体验优化(可选 v1.1.3):UI 线程 ffmpeg/ffprobe 探测后台化(添加文件/点选卡顿)、AudioView 播放器 Dispose、自定义组合 >100% 音量 limiter、XAML 标签列 56px 溢出/强制 Dark 主题打磨;
- 审计中评估后未改项:"暂停→取消"会解冻其它批进程(边缘场景,需按进程状态跟踪,风险>收益,暂缓)。

## 五、工具与令牌

- GitHub token:从 Git Credential Manager 取(`echo "..." | git credential-manager get`),账号 AlLHHH,仓库 push=true;新会话如失效请重新登录 GCM 后重新提取;
- 引擎/模型都在 `发布版\engines\`(部署由 deploy.ps1 管理,勿手拷);ONNX 模型(超分/补帧/音频在 realesrgan/rife/demucs/lavasr 目录,抠图 6 模型+RealESRGAN_x4plus 在 rembg — 打包 exclude 时注意区分)。
