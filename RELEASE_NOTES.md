ALH Pro v1.3.6 — 更新内容(❕最新说明)
====================================

> 📄 重要声明(免责 / 打赏 / 版权许可 / 第三方软件)见仓库根目录 **`声明.md`**;第三方开源许可见 `THIRD_PARTY_NOTICES.txt`。


## v1.3.6(2026-09-13,预设模型调整 + 补帧倍率显示修复,本版最新)

**这一版主要做什么**:两条都是"看着小、实际会误导人"的问题 —— 官方预设「通用画质增强 不含补帧」的超分模型按反馈换成动漫那支;预设摘要把"补帧倍率"显示错一档的 bug 也修了。

**【修复 / 调整】**

1. **官方预设「通用画质增强 不含补帧」改用 realesr-animevideov3**(与「动漫通用」同款模型)。原模型是自转的 realesr-general-x4v3;摘要里当时还印着旧标签「(轻量)」,现已与下拉同名(「通用 · realesr-general-x4v3(5MB · 中)」)。该预设基线提到 **Rev 5**,升级后**自动刷新这一条**,你自己另存的预设一律不动。
2. **修「去重补帧4x」预设摘要显示成 2x**(任何补帧倍率都可能错一档):摘要里打印的是下拉【序号】(0~5)而不是倍率 —— 序号 2(真值 4x)显示成 "2x",序号 0(真值 2x)更会显示成 "0x",会让你以为自己选错了档。现在序号↔倍率收敛成**一份映射**(序号 0/1/2/3/4/5 = 2x/3x/4x/8x/12x/16x),预设摘要、耗时预估、输出规格全部走它,并补了单测钉住("两档映射到同一倍率""漏掉最高档"这类抄写事故以后会被测试拦住)。
3. **16x 补帧现在也会提示「高倍率」**:判断高倍率的条件原先写成"序号是 3 或 4"(=8x/12x),**把 16x 漏了** —— 最高倍率反而没有耗时提示,已改成按倍率判定(≥8x)。
   本版**不改变实际处理结果**:同样的设置下,输出与 v1.3.5 完全一致,修的是"显示与实跑对不上"。

**【安装包】**

- 标准版(不含模型):随包附件 `ALHPro_v1.3.6_Setup.exe`(约 827 MB)
- 完整版(含全部引擎 + 模型,约 2.4GB):百度网盘 https://pan.baidu.com/s/1_heewIWeoPpWQKPJv9blew 提取码 yxfd
- 模型包(安装时可勾选下载,或手动解压到 `engines\rembg`):随包附件 `models_v1.0.zip`

**升级说明**:直接覆盖安装即可,设置 / 预设 / 记录都在 `%LOCALAPPDATA%`,**不需要先卸载**。
## v1.3.5(2026-09-12,补帧引擎重编 + 界面稳定性 + 去重/预设/设置自检修复,本版最新)

**这一版主要做什么**:把补帧(RIFE)引擎用 2025/2026 版 ncnn 源码重新编译(50 系 / 新驱动不再只能等驱动修复),从根上修掉"开了去重、跑到一半界面卡死"(卡住的是界面,任务其实一直在跑),并完成一轮全面自检(预设 / 设置 / 兼容性 / 速度)修复。**建议所有用户更新**。

**【补帧:引擎重编 + 探测加强】**

1. **补帧引擎重编**:用与实拍超分同一套 2025/2026 ncnn 源码重编 RIFE,50 系 / 新驱动不再"只能等驱动修复"。输出与旧引擎数值等价(1080p PSNR 76.9dB、4K 82.1dB,肉眼无差),速度略快(1080p 3.03 秒 → 2.94 秒)。
2. **补帧探测加强**:按你的视频分辨率实测(不再用 320×240 小图),探测画面换成有确定答案的小位移图,判据从 1 条加到 4 条(能查出"出图但是坏帧");探测结论缓存带上引擎指纹与分辨率档。

**【界面:修掉"跑到一半卡死、像任务挂了"】**

3. **"开了去重就卡死"的真因已从根上修**:日志区滚动条与自动折行互掐造成界面布局循环(后台任务一直在正常跑)。四页日志区滚动条改常驻、去掉面板高度动画、滚动改到下一帧执行。
4. **自愈兜底**:万一还有别的成因,一旦检出布局循环会自动停用界面日志刷新(日志继续写文件、任务照跑完),不会再"界面死了只能重启、把任务一起打断"。

**【稳定性 / 耗时】**

5. **引擎被挂死时不再永久卡住**:看门狗强制终止后的"等管道结束"加了 5 秒上限,收不回来就明确失败并提示重启。
6. **显存不足的"自动降分块"以前是空转**(日志写着降档、参数一字没改、只是白跑 3 次),现按 auto → 512 → 256 → 128 真正降档重试。
7. **修"点一次停止就永久变慢"**:取消不再被误判成硬件编码损坏(此前会在本次运行内永久拉黑硬编、之后全走 CPU 软编)。
8. **备用 ffmpeg 缺失会明确预警**:驱动较旧的机器靠它兜底,缺了就静默变慢、日志里毫无线索,现在会直接提示。

**【去重 / 预设 / 设置】**

9. **去重分析全程可见、随时可停**:帧差+SSIM 这条路径此前既不报进度也不响应停止;现在有逐帧进度、日志心跳,点停止立即中断。开去重默认档改「智能检测」。
10. **预设两处 bug**:把列表切成"按名字 / 按最近修改"后点「应用预设」会套用**另一条**;你自建的同名预设会被官方基线**整份覆盖**(现只标记为官方、参数一个字不动)。
11. **预设文件损坏不再静默覆盖你的数据**(先备份 `.bak` + 本次运行只读保护);音频页「记住上次参数」以前永远失效,已修。
12. **设置里的广告开关**改为「本次运行不显示广告」:默认不勾,只对本次运行生效,重启自动恢复显示(仍然没有永久关闭广告的开关)。

**【兼容性 / 模型】**

13. **老 GTX 分档改准**:GTX 10 系(Pascal)与 GTX 600/700/800/900 系分开——GTX 10 系用 ncnn-Vulkan 完全可用,不再一律劝退。
14. **「检测兼容性」不再用小图糊弄自己**:改走与处理流水线**完全相同**的入口(生产帧尺寸 + 真实模型 + 结论缓存),不再出现"小图检测通过、真跑起来才出问题"。
15. **视频页超分模型下拉调整**:轻量通用模型(realesr-general-x4v3,5MB)上移到「超慢」之前、耗时档改「中」;已保存的模型选择会**自动换算**,不会静默变成超慢。

**【图片后处理:⚠ 升级前保存的图片参数预设请重新调整一次】**

16. **六个锐化类档位各自换机理**,不再是一个旋钮拧六遍(两两相关 0.809 → 0.505);「减少杂色」由两档改连续力度;「去雾」「边缘增强」强度重新定标。**预设里存的是数值——数值没变、效果和以前不一样**,请在图片页重新调一次并另存。
17. **4K 导出提速约 19 倍**:「边缘抗锯齿」换成软件自己的实现(4K 实测 4.88 秒/帧 → 0.26 秒/帧);开关与强度不变,观感一致。

**【安装包】**

- 标准版(不含模型):随包附件 `ALHPro_v1.3.5_Setup.exe`(0.81GB)
- 完整版(含全部引擎 + 模型,约 2.4GB):百度网盘 https://pan.baidu.com/s/1_heewIWeoPpWQKPJv9blew 提取码 yxfd
- 模型包(安装时可勾选下载,或手动解压到 `engines\rembg`):随包附件 `models_v1.0.zip`

**升级说明**:直接覆盖安装即可,设置 / 预设 / 记录都在 `%LOCALAPPDATA%`,**不需要先卸载**。

> 本版是在 1.3.4 那批修复的基础上又打的一次包,1.3.4 的内容也一并包含在内。
> **46 条完整细节**(每条都附实测数据与自检结论)见本文末尾「附录:v1.3.5 详细变更日志」,自检报告见仓库 `SELF_AUDIT_20260912.md`。
>
> **如实告知**:本轮自检仍留 1 条**已知未修**的高危项——v1.3.4 公告里宣称过的「设备判定统一到 DeviceRouting」在生产代码里**从未被调用**(只被单测调用),实际在跑的仍是三份重复解析,也就是说**双卡机上「选独显却跑核显」的修复并未真正生效**。改它涉及多卡行为、需要多卡真机验证,故本轮只记录不动手,详情见 `SELF_AUDIT_20260912.md`。

---

## v1.3.3(2026-09-08,稳定性/兼容性/速度增强)

> **这一版主要做什么**:修掉了一批"看起来能用、实际会翻车"的隐性 bug(黑帧、卡死、耗时虚高、GPU 识别错位),同时给视频处理加了实打实的提速,并把出问题时的定位手段做到位。**强烈建议更新**——尤其如果你遇到过:导出卡住只能强杀、超分/补帧出黑屏、明明有 N 卡却走 CPU 很慢、预计时间误差大、或者引擎探测失败。

**【稳定性 / 防翻车】**
1. **探测看门狗(治"处理卡死只能强杀")**:开始前会用 ffmpeg 完整扫一遍视频(查转场/可变帧率/色彩空间),这一步以前没超时保护、连「停止」都停不下来,一旦卡住整条任务就僵死。现在加看门狗(启动静默 90 秒 / 运行静默 120 秒自动掐掉并提示),不会再出现"点了停止也没反应"。
2. **修 x4plus 系超分全黑帧**:个别卡(尤其 RTX 50 系新驱动)用 realesrgan-x4plus 模型时,引擎会**静默输出全黑帧**(退出码 0、毫无报错)。根源是引擎线程档位过高,现封顶 8→6,全黑帧不再出现(超分是吃 GPU 的,减线程不掉速)。
3. **回退帧统一缩放(治"看不出但真的翻车的花屏")**:某批超分失败回退原帧时,现在会先缩放成一致的尺寸再写入,避免混入异尺寸帧导致 ffmpeg 静默产出花屏成片。
4. **硬编探测修复(治"硬编被静默禁用")**:硬件编码探测之前只编 1 帧,被判"无效"而静默禁用,导致明明有硬编却一直走 CPU 软编(更慢)。现多编几帧(≥5),硬编正确入选。

**【速度 / 体验】**
5. **视频超分提速(相同帧只超分一次)**:视频里很多帧内容完全一样(录屏静止画面、动漫一拍二/一拍三)。现在按内容哈希去重,**相同帧只算一次、其余直接复用结果**,是无损提速(输出跟逐帧超分完全一致)。静止素材越多省得越多,长视频能明显缩短处理时间。
6. **预计时间更准(四个阶段都有 ETA)**:以前预计耗时总是不准,根因是"一个时钟被超分独用",把拆帧/去重/补帧的耗时也算进了超分速率。现在每阶段用各自净耗时时钟(扣除休息/暂停),拆帧/补帧/编码也都有准确的剩余时间提示。
7. **赞赏提示更克制**:处理完成后赞助提示更少打扰(冷却调整为 10 小时,且只有你认真看了赞赏码才触发)。

**【识别 / 兼容】**
8. **GPU 识别修正(治"有独显却走 CPU/走错路")**:修复了「注册表枚举」和「引擎枚举」编号可能错位的问题(双卡机 AMD 核显+NVIDIA 独显上尤其明显),以及专业卡(如 RTX 5000 Ada)被误判成 50 系的风险。现在选设备/判断 50 系更准确,不会再"明明选了独显却跑核显/CPU"。
9. **不轻易掉 CPU + 掉时明确提示**:GPU 可用性探测改为 3 次重试 + 更宽松超时(旧 5 秒对冷启动慢的卡会误报 hang),一次驱动抽风不再把独显判成不可用、整段掉 CPU。万一真降到 CPU(会慢很多),会明确提示你"速度会变得特别慢",不再闷头跑。
10. **引擎路由与自检统一**:自检报告给的结论(能用/驱动偏旧/可能不完全支持)和实际走的引擎路线一致,不再"报告说能用、实际却崩"。

**【排查 / 支持】**
11. **设备实际用途诊断日志**:超分实际走 ncnn-GPU / ONNX-DirectML / ONNX-CPU、编码器实测帧率等全部记进诊断包;还加了"注册表枚举 vs 引擎枚举"双枚举对照日志。再遇到"显示有 GPU 却极慢/失败",把诊断包发来就能一眼定位。
12. **自检结论 + 诊断包加强**:自检给明确结论(✅能运行 / ⚠️驱动偏旧 / ⚠️可能不完全支持);诊断包升级为带崩溃堆栈 + 最近任务摘要,排障更快。

## v1.3.2(2026-09-07,视频稳定性加固 + 补帧降级链 + 设备识别修复)

**【视频稳定性(治黑帧 / 黑屏)】**
1. **兼容模式对 NVIDIA 卡也走 ONNX**:视频超分在勾选「兼容模式」后(无论是否 N 卡)直接用 ONNX DirectML 稳定引擎,彻底避开 ncnn-Vulkan 偶发黑帧(Ada 架构实测会出现 ncnn GPU 队列异常 → 黑帧)。此前仅非 NVIDIA 卡才走 ONNX,导致 N 卡用户打兼容模式仍黑帧。
2. **黑帧后不再逐批重跑**:检测到 ncnn-Vulkan 输出黑帧 → 本次视频后续所有批次直接改用 ONNX DirectML,不再每批先跑 ncnn 失败再降级(消除几十次无谓重跑)。
3. **编码器不再优先用不可靠的 QSV**:qsv(Intel 集显硬编)在「独显 + 核显」双卡机上易输出无效文件。现降级为仅当无其它可用硬编(nvenc/amf)时才兜底;hevc 同理。
4. **编码器探测分辨率加大**(320×240 → 1280×720):让"小图能编出有效文件、真实大分辨率却失效"的硬编在探测期就暴露。

**【补帧降级链(治补帧 0 帧 / 黑帧 / 残缺)】**
5. **显卡优先,不自动落 CPU**:补帧固定用选定的独立显卡;失败/黑帧时按「显卡 → ONNX(DirectML)→ 换卡」降级,不会静默掉慢速 CPU 兜底。
6. **修复独显被误判成 CPU**:此前用「编号 < 数量」判断设备是否有效,当显卡编号是稀疏的(如 1、2,数量只有 2)时,编号 2 会被当成无效而错误转回 CPU,导致"明明有独显却报无独显/走 CPU"。现改为按「编号是否在引擎设备表里」判断(与启动自检同一口径),彻底修复。
7. **缺模型报错直白化**:缺哪个模型就显示哪个(如"缺少补帧模型:rife-v4.13"),不再啰嗦"请安装完整版/检查目录"等话。
8. **补帧残缺自动降级**:RIFE 偶发"只输出第 1 帧就 exit 0"(AMD RX 6750 GRE 实测 140→1 帧,间歇性)——此时有帧非黑帧,旧的 0帧/黑帧检测漏过,导致把残缺当年成功。现检测输出帧数远少于目标帧数即触发降级(ONNX→换卡),保证稳定跑完。

**【视频超分 / 引擎探测修复(治"明明能用却提示无法用 GPU")】**
8. **Real-ESRGAN 探测超时放宽 5s→15s**:视频选 Real-ESRGAN 前的 GPU 探测,旧 5 秒超时对"冷启动慢的 ncnn 卡"(如 RTX 4060 首次加载 Vulkan 要 >5s)会误判为"hang"→ 弹「当前引擎无法用 GPU」。放宽到 15 秒,真 hang(50系)仍能拦住,正常但慢启动的卡不再误报。
9. **探测时给用户可见提示**:开始处理前显示"正在检测 Real-ESRGAN 显卡兼容性(约 15 秒)…",避免用户以为卡住。
10. **视频帧 JPG 直走 GDI(治"WinRT 编码不可用"刷屏)**:视频中间帧转 JPG 此前先试 WinRT BitmapEncoder,但它在后台/非 UI 线程会系统性抛 HRESULT=0x88982F41(视频处理必失败)——每次视频处理都刷"WinRT JPG 编码不可用"日志 + 白试一次。现视频帧直接走 System.Drawing(GDI,转 24bppRgb 规避色偏),不再刷日志、更快更可靠。

**【更新提示】**
10. **新版本提示改回顶部横幅**:不再弹模态弹窗(温和不打断),点「去下载/网盘下载」跳转,点 ✕ 本次不再显示;设置保留「不再提示更新」开关。

**【记住上次参数修复(治视频页参数记不住)】**
11. **视频页"记住"修复**:此前 VideoView 缺少"加载完成才允许保存"的守卫,构造/加载期 `SaveSettings` 会把未就绪的下拉 `SelectedIndex=-1` 写进 `video-settings.json`,重开时 -1 不满足恢复条件 → 码率/格式/编码等下拉显示默认(部分记住失效)。现加 `_settingsLoaded` 守卫 + `CollectVideoParams` 对 -1 兜底为 0,根治文件被 -1 污染。
12. **补帧倍率记住修复**:补帧倍率下拉有 6 档(2x/3x/4x/8x/12x/16x),但恢复条件是 `<= 3`,漏掉 12x(索引4)/16x(索引5)——选高倍率重开后回默认 2x。现放宽到 `<= 5` 并兜底,高倍率补帧能正确记住。


**【版本】**
15. 应用版本号由 1.3.1 → 1.3.2。

---

## v1.3.1(2026-09-06,更新弹窗 + 提示/广告区强化)

**【更新弹窗更强(治老版本不更新)】**
1. 检测到新版本 → 延迟 10 分钟弹出**模态升级弹窗**(显示新版本号 + 去下载/网盘下载按钮),取代原来不醒目的小提示条。检测永远指向 GitHub 最新版(如 v1.3.1)。
2. 每次启动都尽力检查到底:被墙自动换镜像端点、多轮重试,直到拿到确定结果(有新版本 或 已是最新)。
3. 点「本次不再提示」→ 本次运行不再弹;设置新增「不再提示更新」开关(勾选后永久不弹,可随时取消恢复)。

**【提示 / 广告动态区强化】**
4. 右侧状态栏提示条放宽(不再截断)且**常驻不可删除**;提示与广告都改为**随机轮播**,并避开最近显示过的 4 条(减少重复感)。
5. 提示/广告数量改**自适应**:在 hint/ 或 ad/ 新建 json 即自动多轮播,无需发版。
6. 广告卡关闭按钮、赞助商角标位置优化,不再压住标题文字。

**【版本】**
7. 应用版本号由 1.3.0 → 1.3.1。

---

## v1.3.0(2026-09-05,自检驱动 + 始终用显卡 + 稳定性)

**【启动自检(全新)】**
1. 首次启动/版本更新后,弹出**设备自检弹窗**:7 项逐个检测(显卡 / 显存·驱动·内存·CPU / 最佳独显 / 可用性 / DirectML·ONNX超分 / 超分补帧引擎 / 各功能模型),逐项打勾。
2. 全部打勾后停 1 秒 → 显示**正式自检报告**(计算设备/推荐设备/显存/内存/CPU/驱动/DirectML/功能自检/引擎模型),带「确定」按钮,点击才放行主界面。
3. 报告里「功能自检」「引擎/模型」用**绿色✓ / 红色✗**标注,哪项缺失一目了然;设置页也显示同一报告,可「重新检测」「打开引擎目录」。
4. 修复"自检误判无 GPU"(缓存加载时设备表为空,导致报告/选卡误判)。

**【始终用显卡,绝不自动转 CPU】**
5. 通用引擎降级链去掉"GPU→CPU"(失败改报错+建议);视频超分黑帧也去 CPU 兜底,GPU→ONNX→还黑直接回退原帧。
6. 计算设备下拉**移除 CPU 选项**,默认=自检**实测最佳独显**;**你手动选的有效独显不被覆盖**。
7. **核显作为"底牌"兜底**(仅当独显全部不可用时才用,并黄色提示处理变慢);50系/AMD 自动走 ONNX(源头避开会黑帧的 ncnn-Vulkan);waifu2x / rife 用新版引擎(兼容 Blackwell)。
8. 看门狗按设备档次自适应:真独显 1 分钟不动即判,核显/CPU 宽容(不误杀天生慢的设备)。

**【性能(吃满显卡)】**
9. 大显存(5070 Ti 等)ONNX 超分**分块显存自适应**(512→768)+ **3 路并行**,GPU 占用与速度显著提升。
10. RIFE 补帧 GPU 探测放宽(5→10 秒 + 重试),正常独显不再被误判切成 ONNX。
11. 补帧 ONNX 宽高补到 4 的倍数(非 4 倍数帧不再静默退化成"复制端点"=补帧无效果)。

**【兼容 / 适配】**
12. HDR / 广色域源自动转 BT.709 SDR(拆帧时 zscale+tonemap,避免偏色/掉信息)。
13. 图片超分加超大图尺寸/内存保护(不再 OOM);MKV 无损音轨不再被强制转 AAC;AVIF 图片可导入(缺系统解码器时黄字提示)。
14. 视频分辨率超 4K / 帧率超 240 时黄色提示,分 3 种(仅分辨率/仅帧率/两者都超);判定按总像素更准(竖版 4K 不误报)。
15. 黑帧兜底链仍黑时回退原帧,绝不把黑帧写进输出。
16. **兼容模式 / 弱设备**:弱设备(无 GPU、核显、显存<6G、内存<8G、核数≤4)黄字提醒建议开启「兼容模式」;「兼容模式」自动降分块/批大小防爆显存卡死,并对**风险卡(非 NVIDIA/AMD)强制走 ONNX 稳定路线**(避开会黑帧的 ncnn),**尊重用户选择的倍率、不乱降**;自检/选卡/看门狗按设备档次自适应,核显作"底牌"兜底并黄字提示。
17. **黄字提醒**:黑帧降级链、显存不足自动降分块、高倍率补帧(4x+)、50系/风险设备走 ONNX、临时盘紧张等均黄字提示,让用户知道"为何慢/在重试/非卡死"。

---

## 一、v1.2.2(2026-09-05,兼容适配加固 + 设备/编码/同步修复)

**【选卡反向 · 双卡机】
1. 修复「选独显跑核显」:计算设备下拉此前回退到注册表顺序(AMD 核显 + NVIDIA 独显机上与引擎真实 -g 编号颠倒)。现同步引擎真实枚举,设备名/数量/推荐编号一律按引擎 -g 编号取,下拉/报告/引擎三者一致。
**【画面冻结 · 视频】**
2. 修复"输出整片冻结/灰屏":PNG→JPG 的 WinRT 编码在部分机型/后台线程稳定失败→每帧被替换成深灰占位帧。改为失败时回退 System.Drawing(转 24bppRgb),输出永远真实画面,不再刷屏逐帧失败日志。
3. 修复 h264_nvenc 报 -22 / "Could not open encoder":内置 ffmpeg 需较新版 NVIDIA 驱动(≥610.00/nvenc API 13.1),旧驱动不兼容。检测到驱动过旧即降级 libx264 并提示更新显卡驱动,不再重复白跑+刷屏。
4. ONNX 补帧 "Parameter is not valid."(输入解码/输出尺寸异常)加防护,报错带帧名/真实原因,不再静默退化成"复制原帧"。
5. ncnn CPU 兜底崩溃(exit -1073741819):标记本会话 CPU 不可用,后续跳过 CPU 改用 GPU,避免反复崩溃拖慢/卡住。
6. 黑帧兜底链(GPU→ONNX→CPU)仍黑时回退原帧,绝不把黑帧写进输出。
**【临时盘 · 长视频】**
7. 超分时逐批删除已消费的补帧帧(峰值不再"补帧帧+超分帧"同时占盘);临时盘预估改 JPG 折算(原按 PNG 高估十几倍误拒长视频)。
8. 修复换盘/换临时路径后,旧盘/旧目录的 imgup_*/alh_* 残留清不掉(累计几十上百 G):现在扫描所有固定盘根目录 + 本软件历史用过的临时根目录。
**【兼容适配】**
9. GPU 检测偶发失败:自检失败但用户已选 GPU 时先重试一次探测,确认仍无 GPU 才降 CPU(不再一次抖动就全局切 CPU)。
10. 引擎探活(超分/补帧 GPU 兼容性)改用 320×240 真实尺寸测试图(原 1×1/64×64 太小会假通过)。
11. DirectML 设备映射改真 DXGI 枚举(而非按注册表名猜),双卡机不再选错卡跑核显。
12. 音画同步:音频 atrim 后 asetpts 由 N/SR/TB 改 PTS-STARTPTS(保留源音频原生时间/间隔,减少音画不同步)。

---

## 二、v1.2.1(2026-09-05,大幅省盘 + 稳定性/修复)

**【省盘 · 长视频临时盘 200 多 G → 约 1/5~1/10】**
1. **视频中间帧 PNG→JPG**:拆帧/补帧/超分后的中间帧统一用 JPG(质量 0.96,近无损),合帧读 JPG。长视频处理中占盘从 200 多 G 降到 20~40 G;引擎输出仍读 PNG,画质几乎无损。
2. **临时盘提前预估(分辨率感知)**:不再按固定 3MB/帧低估,超长视频处理前会**预估所需空间并提前警告/中止**,不再偷偷跑满磁盘。
3. **强杀残留自动清理**:临时目录自动选"剩余最大盘"的残留,启动时也一并清除。

**【稳定性 / 修复】**
4. **加图/加视频不再卡死(未响应)**:图片解码、视频 ffprobe 挪到后台线程;处理进度回调加 100ms 节流并去掉 O(N²) 计数——长视频不再把 UI 线程塞死。
5. **修复:自定义输出分辨率 + 不超分 → 输出全黑**(GDI+ 文件句柄共享冲突);**已去掉视频页「自定义分辨率」选项**(旧设置自动回退 2x)。
6. **修复**:中间帧 JPG 重编码失败改用同尺寸占位帧(不再复制损坏字节);waifu2x 1x 无降噪输出统一 .png。
7. 图片超分分块改按"安全渲染"墙;GPU 显存不足先减半分块留 GPU;Real-ESRGAN 走 ONNX 判定放宽;预处理降噪设备路由走 ONNX。

**【备注】**
8. 1.2.1 的目标:处理长视频不再"爆盘 + 卡死"。若想极致画质,后续可加"无损模式(中间帧 PNG)"开关(本版默认 JPG 省盘)。

---

## 三、v1.2.0(2026-09-04,官方预设 + 超分质量 + 兼容/修复)

**【新增官方预设(作者 AlL.H 制作,新装/更新后自动带上,无需自己保存)】**
1. 视频 **「去重补帧4x」**(视频页官方预设)
2. 图片 **「通用变清晰」**:细节/锐化/降噪均衡的最常用"变清晰"档,一键出图。
3. 图片 **「清晰MAX」**:细节/锐化/抗锯齿拉满,适合想明显更锐的照片。
   (原有官方预设:视频「通用画质增强 不含补帧」「动漫通用」保留不变。)

**【预设与界面】**
4. **预设导出多选模式**:点「导出」后每行变复选框(默认不选),可勾选多个一起导出;底部「确定导出 / 取消」;导出文件名带预设名。
5. **预设导入格式校验**:图片(.alhimg)与视频(.alhpreset)严格区分,误选另一类型会明确提示,不再静默错读参数。
6. 图片页新增「参数预设」区(格式与视频页分开)+「重置所有参数」+「记住上次参数」。
7. 音频页新增「记住上次」「重置所有参数」(与图片/视频页一致)。
8. **图片超分增强算法重写**:真去雾(DCP)、真去模糊(Richardson-Lucy),并区分"清晰 / 锐化 / 钝化蒙版 / 细节增强 / 边缘增强 / USM"的不同作用,不再"一锅增强"。
9. 图片/视频进度条旁显示输出规格(分辨率 / 帧率)。
10. 视频页:超分模型随「启用超分」开关联动置灰;「高级参数」里补帧输出帧率基准提供黄色提示;撤掉引擎目录模式下无效的「时间步」。
11. 果冻修复:移除易给真实照片带来涂抹的「减少果冻」;开启运动模糊/画面去抖时黄色提示会增加导出时间。

**【窗口与兼容】**
12. 窗口最小化/恢复时顶部不再变白、右上角三键不再偏移(强制深色标题栏);窗口还原按当前 DPI 换算并钳制到工作区(不同缩放屏不被压小/跑出屏幕)。

**【修复】**
13. **超分"拼贴一块一块"接缝**(ONNX 路径,50 系/无独显/CPU 走 ONNX 时):分块倍率对齐 + 羽化融合,消除马赛克/硬接缝(已用真实模型测图验证)。
14. GPU 显存不足:先自动减半分块留在 GPU 重试,不再直接掉到 CPU(慢数倍)。
15. 图片超分分块改按"安全渲染"墙:小显存(6~8GB)不再 OOM,更大图自动走无缝分块。
16. Real-ESRGAN 走 ONNX 的判定放宽:任一 ONNX 模型存在(通用/动漫)即走 ONNX,避免 Blackwell 硬走会崩的 ncnn-GPU。
17. 图片「预处理降噪」设备路由:高风险设备(Blackwell/无独显)自动改走 ONNX 稳定引擎。
18. 50 系补帧 NVENC 编码 exit-22 处理加固。
19. 官方/本地预设时间步统一 0.5(旧预设自动归位)。

---

## 四、v1.1.3(2026-09-03,提速 + 兼容性 + 临时文件)

**【新增】**
1. **用户协议弹窗**:首次启动需同意协议(含免责声明)才能使用;「关于」页可随时回看已同意的协议。
2. **设备自检报告升级**:按本机真实显卡架构(50系/40系/30系/20系/老GTX/AMD/核显/Arc)生成针对性解释,不再是笼统"50系/AMD怎么怎么样"。
3. **设备检测加固**:过滤虚拟显卡(Oray/GameViewer/基本显示适配器),防止把无计算能力的虚拟卡当"可用的GPU"推荐。

**【提速 · 超分】**
4. **消除超分"一块一块"接缝 + 黑帧**:图片/视频超分改用 `-t 0`(关闭引擎内部tiling),实测 4060 上不再出现"方正拼贴"接缝和 vkQueueSubmit 黑帧。
5. **大图超分提速**:能整图直跑的图(≤约3840px)不再分块(省去逐块启动引擎开销);超大的图也改为一次目录批量启动引擎处理所有块,而非逐块启动。实测大图提速明显。
6. **超分开放模型选择**:图片/视频超分现在可选各引擎模型(显示模型名+体量+快慢);视频 Real-ESRGAN 默认用 realesr-animevideov3(1MB,比 x4plus 快约3.5倍)。

**【临时文件】**
7. **临时文件占用优化**:补帧完成后立即清源帧、超分完成后清补帧帧、级联补帧中间目录用完即删,处理中临时文件峰值显著下降(修复反馈"临时文件100多G")。

**【其他】**
8. 移除视频页「推荐配置(一键)」按钮(避免"不会选/选错模型"造成的困惑)。
9. 音频「真超分辨率」改名为「AI 升采样率」(更直白);说明文字同步对齐。
10. 设置页设备说明文字修正:音频升采样率实际用CPU(非DirectML)的准确描述。

---

## 五、v1.1.2(2026-09-02,基于真机反馈)

**【新增】**
1. **新版本更新弹窗**:每次更新后首次启动自动展示——顶部"来自作者的话"(金色强调),版本可下拉切换,正文全宽显示
2. **一键导出诊断包(zip)**:设置页一键打包 日志+设备信息+设置,发给作者即可快速定位问题(反馈闭环)
3. **视频页「推荐配置」一键**:动漫补帧 / 真人视频 / 快速预览(治"不会配导致效果不佳")
4. **GPU 自动选择升级为【实测验证】**:引擎枚举的设备按优先级逐个 1×1 真机探测,选第一个真的能用的独立显卡——三卡机(Intel 核显×2 + RTX 5060)以前会误选核显,现在自动选独显
5. **界面精简**:左侧边栏改为纯文字(全部图标去掉),更新日志并入启动弹窗,界面更干净

**【修复 / 加固】**
6. 手动选 CPU 时 waifu2x/realesrgan **自动改走 ONNX**(稳定,画质一致;ncnn CPU 模式在部分机器会崩)
7. DirectML 不可用时**明确提示更新驱动**(RTX 50 系需较新驱动;不再无声掉到 CPU 慢几倍)
8. 高倍率补帧预警:核显/显存不足 8GB + 4x 以上(或目标 ≥90fps)→ 开始前弹窗建议改 2x
9. 修复:兼容提示日志刷屏(只记一次)、GPU 自检 GDI+ 异常误报"无 GPU"、诊断包解压失败(改为完整写好再交付)
10. 修复:AMD 核显识别(680M/780M/Vega 等 APU 核显名)不再被当成独显——混合显卡机型不会误选核显
11. 图片超分去掉「不放大 (1x)」选项(只剩 1x 超分/2x/3x/4x,旧设置自动映射)
12. 使用教程全面更新至 v1.1(新增音频处理、推荐配置、GPU 路线说明、抠图模型清单修正)
13. **修复:视频临时磁盘预检单位错配(历史"1450 帧爆盘"根因)**——预检此前几乎从不触发,现在按字节精确预估,不足会提前中止并明确提示
14. **音频 AI 分离/增强接入显卡加速**(DirectML)——之前固定用 CPU(一首歌几十分钟),现在优先显卡、无显卡自动 CPU
15. 修复:抠图模型未安装时点「开始处理」页面锁死(现在直接弹明确提示,不再只能重启)
16. 修复:手动选 CPU 时被自动回退到 GPU(ONNX 路线尊重用户设置);RIFE ONNX 会话按设备缓存(多分辨率混合不再被小帧 CPU 会话拖慢);取消视频处理时先等已启动批次收尾(不再残留/报 IO 错);音频过长时提前预估内存并提示(不再处理到一半整体崩溃)
17. **模型安装引导强化**:模型包内含中文「安装说明(必看).txt」;安装器下载模型前先确认、失败可跳过(不影响软件安装);软件内「AI 抠图」页模型缺失提示条新增「查看安装方法」图文步骤;提供「完整版(含模型)」整包(解压即用,网盘/社区分发)
18. RIFE ONNX 补帧分块(4K 低显存不再爆,真机实测 1024×768 2.2 秒);DirectML 运行期失败即记录并绕开(不再每帧重复失败卡顿)

---

## 完整版(含模型)下载

安装包不含 AI 抠图模型(1.4GB);嫌下载/安装模型麻烦的,用**完整版**(约 2.4GB,双击安装,装完所有功能直接可用):

**百度网盘:[点此下载 ALHPro_v1.1.2_完整版_Setup.exe](https://pan.baidu.com/s/1_heewIWeoPpWQKPJv9blew?pwd=yxfd) 提取码:`yxfd`**

---

---

## 六、v1.1.1 修复版(2026-09-02,由上版升级可见)

1. 修复:视频处理选「Real-ESRGAN」一点「开始处理」就崩溃(0x8001010E 跨线程)——双卡机/AMD 核显机型必现
2. 修复:「GPU 出黑块 + CPU 模式不可用」无限重试死循环(RTX 2080 真机 4 分多钟 10+ 次循环,输出还带黑块)——自动改用 ONNX 稳定引擎
3. 修复:大视频处理到一半"磁盘空间不足"失败(真机 1450 帧爆盘)——临时文件自动选最大盘 + 处理前估算不足直接中止 + 批次帧处理完立即删除
4. 修复:音频真超分辨输出"时长砍半 + 变单声道"(8 秒只出 4 秒)——按 WAV 头正确逐声道处理
5. 设置新增「临时文件位置」:自动(推荐)/ 指定位置,任务完成自动清理 + 一键清理(弹窗反馈清理数量与空间)
6. 设备兼容加固:双卡机 DirectML 设备号按显卡名映射;waifu2x 安全网扩展覆盖 AMD/Intel 显卡
7. 音频页"开始处理"进度提示完善
8. 图片/视频页去掉工具栏冗余格式提示

---

## 七、大更新说明(由 v1.1.0 引入,一并收录)

**v1.1.0 是"音频 + 50 系适配 + 许可合规"的大版本:**

9. 全新「音频处理」页面:
   - 超分:电话/老录音等低采样率源(8k/16k/22k/32k)用 AI 补高频,升级到 48kHz(听得出变清晰)
   - 增强:人声/伴奏分别优化后重混,咬字更清晰、层次更好
   - AI 分离:提取人声 / 去人声(卡拉OK)/ 分离导出两文件 / 自定义组合(可同时输出多轨 + 人声伴奏各自调音量 0~200%)
   - 链式处理:分离→超分→增强 一路到底,最终只出你要的那几个成品文件
   - 输出采样率跟随源:48kHz 源处理后仍是 48kHz
10. 50 系显卡(Blackwell)彻底适配:自动走 ONNX 稳定路线、waifu2x 官方 2025 新版引擎、补帧自动 ONNX、处理前 1×1 实测
11. 移除 Real-CUGAN:官方仓库未附开源许可且无授权联系渠道,为合规分发不再包含(超分仅保留 MIT/BSD-3 许可的 waifu2x 与 Real-ESRGAN)

---

## 已知限制(诚实说明)

- 音频 AI 分离优先显卡(DirectML)加速;无显卡/驱动不兼容时用 CPU(约 1.5 分钟/分钟音频)
- 超分只对低采样率源(8k/16k/22k/32k)真的有效;44.1k 音乐已全频带,无需超分(用「增强」)
- 视频超分/补帧在老电脑上慢是物理限制,建议 waifu2x + 1x~2x

作者 AlL.H

## 附录:v1.3.5 详细变更日志(46 条,含实测数据)

> 以下是上面摘要的完整版,逐条带实测数字与原因说明;软件内「更新详情」只显示上面的摘要,不读本节。

**【稳定性 / 防"卡死/极慢"】**
1. **GPU 设备被摘除/挂死时熔断(治"超分几小时")**:当显卡被系统摘除(DXGI 887A0005/887A0006)时,立即停止所有 GPU 尝试,超分批次快速回退为源帧缩放——不再逐帧反复重试、也不再掉慢速 CPU。这类错误在本进程内不可恢复,只有重启软件才行,现在会明确提示。
2. **修空闲显存探测短路(治"显存护栏失效")**:v1.3.3 里一个短路逻辑导致空闲显存永远探测不到(诊断包显示"未实测"),于是并发/批大小不按显存控制,8GB 小显存卡上多路并行 DirectML 容易耗尽显存 → 设备被移除。现已修复,显存护栏恢复正常。
3. **GPU 重试闩锁修正(治"宣传能重试却只重试一次")**:GPU 引擎失败"同设备重试 3 次"的静态标志之前置位后永不复位,整个进程其实只兑现一次。已改 Interlocked 深度计数 + finally 归还。
4. **批次起始槽位竞态(治"进度跳来跳去")**:批次进度之前读一个只在结束时累加的计数器(并行时来回跳、ETA 基准错)。改用确定性的前缀和,进度稳定。

**【设备判定统一(治"选独显却跑核显/CPU")】**
5. **设备判定统一到可测层,选独显绝不跑核显**:把"计算设备编号解析"抽成可单测的纯函数(DeviceRouting)。此前图片页(UpscaleView)/抠图页(CutoutView)用「编号<数量」判设备,在"注册表枚举 `[#0 AMD核显 | #1 NVIDIA]` 与引擎枚举 `[#0 NVIDIA | #1 AMD]` 顺序相反"的双卡机上会选错卡,明明选了独显却跑核显/CPU。现在按「编号是否在引擎设备表 + 是否核显」判定——**选中的是核显、表里另有独显时自动换到独显;设置值时撞号到核显也会一并纠正**,并留痕日志。视频页此前已改,本版把图片/抠图/设置读回也统一。
6. **设备表非空绝不落 CPU**:解析设备时若设备表非空(确实有可用设备),即便没有推荐值也取表内设备,不再静默掉成 -1(CPU)。

**【修复"选了没生效"的隐性 bug】**
7. **补帧倍率 switch 漏 12x/16x(治"选 16x 实际只 2x")**:补帧倍率下拉有 6 档(2x/3x/4x/8x/12x/16x),但 switch 只映射了前 4 档,选 12x(第5档)/16x(第6档)会落回默认 2x。已补齐(共 4 处),选高倍率真正生效。
8. **音频/视频熔断分表(治"音频处理把超分/补帧搞崩")**:DirectML 瞬时失败连击表此前所有业务共用一个(音频、图片超分、视频补帧都记在同一个设备号上)。音频(HT-Demucs)显存压力与图片模型完全不同,连吃几次失败会把图片超分/视频补帧判成"设备不可用",或音频成功一次就把视频攒的连击清零。已按业务线分表,互不污染。

**【体验 / 预警】**
9. **输出规格超限提示(不弹窗,内联红字)**:导出分辨率超过 4K(宽>3840 或 高>2160)**且**帧率超过 240 时,开始处理前弹窗警示(可能占极大量显存/磁盘、处理非常慢甚至失败),建议降低倍率/分辨率/帧率;超 4K 时在输出规格行旁红字提示(弹窗容易被打断,改用不打扰的内联提示)。
10. **输出规格行统一格式 + 加占用估算**:输出规格统一写成「输出: W×H(源 w×h ×N)」,倍率直接看得到,不再重复写"Nx超分";并估算「临时帧≈X GB · 成片≈Y GB」(成片按 0.1 bit/像素/帧粗估,自定义码率直接用你填的值)。
11. **补帧进度逐帧显示(治"进度不动/跳")**:ONNX 补帧路径的进度文案改成了解析器能认的标准格式,且帧号只从消息取、单调不回退——补帧进度现在能逐帧更新了。
12. **批次剩余时间语义修正**:整批剩余时间不再贴到单个视频项上混淆视听,拆成"整批剩余"和"本片剩余"。

**【错误提示更友好】**
13. **广告/提示拉取失败的日志不再刷看不懂的英文**:网络波动/被墙时只提示一句"在线广告暂时拉取不到,不影响使用",而不是一堆 `HttpClient.Timeout`。
14. **任务日志带版本号**:处理的日志第一行会带上当前版本,排查"哪个版本出问题"更方便。
15. **设备编号路由抽到可测层**:计算设备编号解析、探测色差判定等抽成纯函数并补上单测,GPU 编号错位等以前难以真机验证的问题现在能被单测钉住。

**【图片页后处理:参数调整 + 老预设请重新调整】**
16. **「减少杂色」由"两档开关"改为连续力度**:此前这个滑杆其实只有两个档位——强度 1~50 是同一种效果、51~100 是另一种,**同一档内怎么拖都完全没变化**,而且一开就是最强力度,会连 1 像素宽的细线一起抹掉(发丝、细描边会整段消失)。现在改为按强度连续调节:20~30 只轻抹颗粒,70 以上才接近原来的"彻底"档。**原来的最强档对应现在的强度 100,喜欢旧效果的把滑杆拉到 100 即可。**
17. **⚠ 升级前保存的图片参数预设,请重新调整一次**:预设里存的是数值,而这一版改了六处数值的强度含义——「减少杂色」(两档改连续)、「去雾」「边缘增强」(强度重新定标)、「锐化」「清晰」「保留细节」(换了机理,见下条),**数值没变、效果和以前不一样**。升级不会修改也不会删除你的任何预设,请在图片页「使用预设」里重新调一次并另存。
18. **六个锐化类档位「各自换机理」——不再是一个旋钮拧六遍**:此前"锐化/清晰/钝化蒙版/保留细节/细节增强"五个档本质是同一个算子(反锐化)只换了半径,改动方向两两相关 **0.83~0.98**,一起开等于同一个旋钮拧好几遍、只叠白边。现按机理重新分工:
    - **保留细节** → 真·自适应直方图均衡(CLAHE):提局部明暗层次,**天生不出白边**(实测白边像素 ≈0),与其它五档的改动方向**相关≈0(完全正交)**;它提的不是锐度,拉普拉斯指标看不见,得看图。
    - **锐化** → 亮度域小半径锐化 + **按边缘强度加权**(强边缘多给、平坦区少给),不会再出彩色描边,同强度下白边只有旧写法的一半。
    - **清晰** → 大范围局部对比,但**只作用于中间调**(暗部与高光几乎不动),提通透感而不是硬锐度。
    - **细节增强** → 高通 + **软门槛**,幅值小的(噪点)不放大,只放大成形的微纹理 —— 六个档里同等清晰度下代价最小。
    - **钝化蒙版** → 保持经典做法(逐通道 + 阈值保护),作为标准参照。
    - **去模糊** → 反卷积核改成**连续变化**(此前强度 60 处核半径从 1 跳到 2,效果有隐藏台阶),并保留"超 900 万像素自动降级"的提示。
    实测六个档两两相关 **均值 0.809 → 0.505**,不再互相顶替。**搭配建议:一次最多开 2 个**(日常「细节增强」+ 想要通透再加「保留细节」)。
19. **各档说明文字按实测改写**:此前 10 档里有 13 处说明与实际做的事对不上(例如「细节增强」写着"温和,不易放大噪点",实际它是半径最小、最容易放大噪点的一档;「去模糊」写着"最后再补一点锐化",代码里没有这一步),悬停提示现已逐条改成与实现一致,并标注了实测得到的建议区间与副作用。
20. **诊断包的 ncnn 实测结论不再显示"未测"**:此前诊断报告里的引擎探测结论是"按版本缓存的快照",生成时间早于任何一次真机探测、之后从不刷新,所以**装完新系统/重装后永远是"未测"**(纯 NVIDIA 非 Blackwell 机型甚至从不落盘结论);另外探测结论文件是 `.txt`、不在诊断包的收集范围内。现在导出诊断包时会先实测一次、写入实时结论,并把探测结论文件一并打包。
21. **「去雾」强度重新定标(滑杆终于整段有用)**:实测这个档只有在很窄的一段里有效——原来强度 36 以下几乎没作用,超过 56 开始把画面压死(56 档 14% 的像素被压到纯黑、70 档 61%、拉满 100% 全黑)。现把强度整体压到实测有效区间:**整段滑杆单调有效,拉满 = 最彻底**(最好用的位置大约在 87)。注意:**没有雾的图任何强度都不要开**,它只会把画面变灰变脏。
22. **「边缘增强」重做机理(档位保留,不再只出白边)**:这一档原先的两项计算都"只加正值",于是**只把边缘往亮的方向推** —— 实测只出白边不出暗边(暗边始终 ≈0.05%),是十档里最差的一档。现改用**对称的拉普拉斯锐化**再按边缘强度加权:暗侧变暗、亮侧变亮,天生对称,并且只动成形的边缘(平坦区/噪点不动)。实测(强度 100):干净 waifu2x 基底 PSNR 损失 **−10.99dB → −3.65dB**、白边像素 **0.027% → 0.002%**、细节反而更多(156% → 172%);干净 Real-ESRGAN 基底强边缘白边 **13.18% → 4.14%**、细节 138% → 196%。

**【视频导出提速 + 几个"莫名变慢"的坑】**
23. **4K 导出明显提速(「边缘抗锯齿」换了实现)**:这一档原先用 ffmpeg 的 sab 滤镜,实测在 4K 上和其它滤镜串在一起会把整条后处理链拖到 **4.88 秒/帧**(把这一档去掉只要 0.10 秒/帧,差约 48 倍)。现改成软件自己的实现(与图片页同一套)**按帧多核并行**:4K 实测 **0.26 秒/帧,快约 19 倍**;并且与"中间帧转 JPG"这一步合并,不额外多一代画质损失。**开关和强度都不变,输出观感一致,只是快得多。**
24. **日志新增「编码阶段拆分」,瓶颈一眼可见**:过去"编码/封装 X 秒"里其实混着 ffmpeg 的后处理滤镜(同一个进程里跑),于是出现过"硬件编码只有 1fps"的假象 —— 实际编码器有 15~20fps,是滤镜在吃时间。现在会把**滤镜**(抽样实测 秒/帧 × 帧数)和**编码/音频/封装**分开报,滤镜占大头时还会直接标注"⚠ 滤镜占了大头,瓶颈不是编码器"。
25. **修「点了一次停止就变慢」**:编码过程中点「停止」时,软件把"你取消了"误当成"硬件编码坏了",于是**在本次运行内永久拉黑硬件编码**,之后所有任务都静默改用 CPU 软编(慢数倍),必须重启软件才恢复。现已把"取消"和"失败"分开处理。
26. **备用 ffmpeg 缺失会明确提示**:内置主 ffmpeg 的 NVENC 需要 NVIDIA 驱动 ≥610;驱动较旧的机器靠 `engines\ffmpeg8\` 里的备用 ffmpeg 兜底(实测 572 驱动下主 ffmpeg 直接报"需要 610 以上")。该目录不在仓库里、部署时容易漏拷,漏了就静默变慢 —— 现在缺了会直接警告"本机将使用 CPU 软编"并说明怎么补。
27. **加固「JPG 序列硬件解码」探测**:原先只用 1 帧试,而实测"多帧序列 + NVDEC 解码"这条路可能直接卡住不复返(20 帧的命令永不结束,同命令去掉 NVDEC 只要 0.67 秒)。现改为 **8 帧序列实测 + 6 秒硬超时**,超时即判不可用 —— 就算某台机器上真会卡,最多损失 6 秒(而且每次启动只探一次),不会再让整个任务无声挂死。
28. **「去模糊」加过冲钳制 + 减少迭代(档位保留)**:反卷积是"病态问题" —— 输入本来不糊时,它会把噪点当细节放大并产生振铃/白边(实测干净基底强度 50 的白边像素到 22.49%)。现在每轮迭代后把结果**夹回观测图的局部动态范围**(标准做法),并把迭代上限从 10 降到 6(多迭代只增加振铃与耗时)。实测(强度 100):干净 Real-ESRGAN 基底白边 **2.211% → 0.552%**、PSNR 损失 **−5.10dB → −2.50dB**;1080p 单帧也更快了。

**【补帧(RIFE):引擎重编 + GPU 实测探测加强】**
29. **补帧引擎重编:50 系/新驱动不再"只能等驱动修复"**:补帧用的 RIFE 引擎此前是 2022 年的上游二进制,它链接的 ncnn 里**完全没有**新显卡需要的 Vulkan 兼容处理(静态指纹实测:老引擎 `VK_EXT_robustness2` / `VK_KHR_cooperative_matrix` 命中数 **全是 0**)。现用与之前 Real-ESRGAN 重编**同一套 2025/2026 ncnn 源码**重新编译了一份,指纹与那份已实测可用的引擎**完全一致**(robustness2 = 2/2、cooperative_matrix = 364 命中)。**结果与旧引擎数值等价**(同机同模型逐帧比对:1080p PSNR **76.9dB**、4K **82.1dB**;亮度/结构一致,肉眼无差),并且更快一点(1080p 3.03s → **2.94s**,4K 4.13s → **3.89s**)。程序会**优先用重编版**,机器上只有老引擎时照旧能用(不删功能)。
    > 重编过程里修掉了上游源码两处"只在 MSVC 下能过"的写法,否则编出来的引擎会**退出码 0、却输出全黑帧**(最坏的那种失败):①模型路径用 `swprintf(L"%s/...")`,MSVC 把 `%s` 当宽串、MinGW 按 C 标准当窄串,路径被截成 `r/f.param` → 模型根本没加载;②上游还引用 `opt.use_shader_pack8`,而现代 ncnn 已彻底移除 pack8。两处都已在源码注释里写清原因与证据。
30. **补帧 GPU 探测:按你的视频分辨率实测,且"出图但是坏帧"不再漏掉**:
    - **探测尺寸 = 源分辨率**(原来固定 320×240):320×240 只占 1080p 的 1/27、4K 的 1/108 像素,**小图能跑不等于真帧能跑**。现在按你的视频尺寸实测,超时也按面积给(1080p 以下 15 秒、1080p 及以上 25 秒;实测 4K 最重模型热态约 7 秒,冷启动约 8 秒)。
    - **探测画面换成了有确定答案的那种**:原来是"纯黑帧 → 纯白帧"硬切 —— 对补帧算法这是**病态输入**(没有真实运动、只有内容突变),实测好引擎插出来的帧均值只有 **2.4**(近黑)、而且随分辨率乱变(1080p 74.4、4K 90.3),**"正确结果该是多少"根本没定义**,所以旧探测只能查"三通道是否均衡",出黑帧、出糊帧一概查不出来。现改为"水平渐变 + 同一渐变右移 2 像素"(等于一个真实的小位移),正确结果有确定预期:均值≈两输入均值、方差≈输入方差。
    - **判据从 1 条加到 4 条**:通道均衡 / 不是近黑或带状近黑帧 / 亮度与预期对得上 / 画面没被抹平(结构还在)。**实测 13 个模型 × 新老两代引擎全部通过,余量极大**(均值偏差 ≤0.24 对容差 24;方差比 1.00 对门槛 0.25)。
    - **探测结论的"记忆"更严格**:结论缓存键里加进了**引擎文件指纹**(换引擎就重新实测,不会把老结论套到新引擎上)和**分辨率档**(1080p 上测出来的结论不会拿去给 4K 任务背书)。

**【去重:不再"看着像卡住"】**
31. **去重分析全程可见、随时可停**:「手动-帧差+SSIM」这条去重路径此前**既不报进度、也不响应停止** —— 长片/多帧素材上界面一动不动、点停止也停不下来,只能判成"卡死"。现在:①进度每 16 帧刷新一次(界面显示"第 N 帧 / 共 M 帧(已判重 X 帧,已用 Y 秒)");②**写进日志**(每 5 秒一条心跳 + 一条"开始"、一条"完成:共 N 帧,判重 M 帧,用时 X 秒");③点「停止」能立刻中断(自适应/帧差+SSIM/镜头运动三个检测器都接上了取消)。有了开始/心跳/结束三条,以后遇到"卡住"能直接看出停在第几帧。
32. **修一条"看门狗赢了却还卡着"的死路**:子进程被驱动挂死时,看门狗的"强制终止"只是**请求** —— 进程若卡在内核态,它不会真退出、管道也不会关,而旧代码在杀掉之后会一直等它把输出读完(等 EOF),于是**任务永久卡在那个阶段**(日志已经打了"已强制终止",界面却再也不动)。现在最多等 5 秒:等不到就放弃输出继续走回退链;若进程确实收不回来,则明确失败并提示重启(不会再拿"可能还在写同一个帧目录的旧进程"去重跑,避免新旧进程交叉写出错帧)。

**【视频页「超分模型」下拉:顺序与耗时档调整】**
33. **轻量通用模型上移到「超慢」之前,耗时档改为「中」**:自转的 `realesr-general-x4v3`(5MB)原先压在下拉最末尾、还标着「快 · 轻量通用」,很多人根本看不到它。现在它排在红字「超慢」的上面,标签写作「通用 · realesr-general-x4v3(5MB · 中)」,不再出现"轻量通用"这类后缀。**已保存的模型选择会自动换算一次**(老序号 3=轻量 → 新序号 2),不会出现"我明明选的轻量、升级后变成超慢(慢 17 倍)"这种情况;官方预设「通用画质增强 不含补帧」也同步刷新到新基线(Rev 4)。

**【修"处理中突然报错/看起来卡住"——界面刷新不再拖死任务】**
34. **日志刷新与自动滚动加了保护(治"跑到一半弹错误框、任务像卡住")**:处理过程中每次刷新日志行、自动滚到最底部,都会调用系统的滚动/文本控件(ScrollViewer.ChangeView 那一类)。这台 50 系笔记本上**一天内崩了两次**(2026-09-12 20:35 与 21:20,异常是 `COMException 0x80070490 找不到元素`,另一次是 `LayoutCycleException`),两次都紧跟在"刷新日志行"之后,而且**只在开了去重时出现** —— 开了去重日志行更多、刷新更频繁,撞上布局的窗口就更多。现在:①这些刷新动作全部包上保护,**刷不动就少刷一次,任务照跑**;②**整个进度刷新回调**再兜一层(任何一项界面刷新失败都不会再冒到"未处理异常");③「高级参数 / 手动去重面板」的展开收起用的是**高度动画**(布局循环的经典来源),它的开始与收尾回调也全部加上保护,失败就退化成直接显示/隐藏;④崩溃诊断里新增一行「最近界面操作」—— 这类异常有时不带调用堆栈,有了它下次一崩就能直接指到是哪一处控件调用。
35. **打开去重时默认档改为「智能检测」**:此前一开去重,默认停在「手动模式」(还要自己挑算法、调一堆滑条),而三种模式里智能检测才是该默认的那个(自动识别拍数;**识别不出来就原样保留、不乱删帧**)。现在启动默认与「重置」按钮的默认都改成「智能检测」。

**【自检发现并修掉的两处"看着没问题其实不靠谱"】**
36. **设置里的广告开关换了思路,默认不再替你做主**:原来那一项叫「本次运行显示广告」、**默认勾选**,看着像"一个开着的功能",想清净还得每次先取消勾选。现在改为**「本次运行不显示广告」、默认不勾**(广告照常显示,要安静自己勾一下)。勾选**只对本次运行生效,重启软件自动恢复显示**(不写进设置文件)——这也是"本次运行"四个字的原意;软件里仍然**没有永久关闭广告的开关**。相关的合规说明与隐私政策文字已同步改准。
37. **「检测兼容性」不再用小图糊弄自己**:点「开始处理」时那次 Real-ESRGAN 兼容性检测,原先用的是**320×240 小图**探测(15 秒),而这个项目自己实测过的失败形态恰恰是"**小图能跑、真帧尺寸才静默出空帧/黑帧**(退出码还是 0)"—— 也就是说小图通过 ≠ 能用,等于给了你一个不可靠的 OK,真问题要等跑起来才暴露。现在改成调用**与处理流水线完全相同**的入口:纯 NVIDIA 非 50 系走快速通道(秒回),50 系/AMD/Intel 按**生产帧尺寸 + 真实模型**实测,结论按「引擎|GPU|模型」缓存(流水线随后直接命中缓存,不会重复探两遍)。

**【本轮全面自检发现的问题(都已修)】**
38. **预设:两条会让你"莫名其妙"的 bug 修掉了**:①把预设列表切成「按名字」或「按最近修改」再点「应用预设」,套用的其实是**另一条预设**(显示顺序和取数顺序用了两份不同的列表)—— 现在两处共用同一个排序来源;②**你自己攒的同名预设会被官方基线整份覆盖**(比如你自己存了一条叫「动漫通用」的,启动后参数被官方那套顶掉,日志还写着"用户自建预设未动")—— 现在同名但不是官方的,只标记为官方、**参数一个字都不动**。另外:预设文件万一损坏(写入被截断/手工改坏),以前会被内置预设**整份覆盖**导致你的预设无声消失,现在会先备份成 `.bak` 并**本次运行拒绝写入**,宁可这次列表是空的也不覆盖你唯一的那份数据。
39. **显存不足的"自动降分块"以前是空转**:日志写着"分块 512→256 重试",实际参数一字没改,只是拿同样的参数再跑 3 次 —— 8GB 小显存卡(如 RTX 5060 Laptop)上等于白等然后降级。现在真降:`auto → 512 → 256 → 128`,正常路径与原来逐字一致(auto 分块)。
40. **音频页的「记住上次参数」以前永远失效**:页面构造时 XAML 的默认值会先写一次设置文件,把用户存过的设置覆盖掉,随后再"恢复"读到的就是这份默认值。已修(与图片/抠图页同一套做法)。
41. **勾了去重却可能被静默关掉**:恢复设置时,去重模式/动漫档位的合法范围写成了旧版本的档位数(3~5 也放行),越界后下拉被清成 -1 → 运行时等于"去重关闭"。现在按真实项数校验,越界回退到安全档(智能检测/一拍二)。

**【降级链与自检:体检结论】**
> 引擎换成 2025/2026 重编版之后,很多"因为旧引擎的毛病才存在"的兜底与自检该重新审视了。这轮逐条清点了一遍(完整结论留档在 `SELF_AUDIT_20260912.md` 第⑥节),给用户看的结论如下。
42. **确认仍然必要、不要动的那些**(都有实测依据):黑帧防御链(整帧近黑 + **带状近黑** + "只有源帧真的是黑场才豁免"的按帧对应判定)、RIFE 探测的"渐变+右移 2 像素 / 24bpp / 四条判据"、探测结论按**引擎指纹 + 分辨率档**缓存、设备被摘除(887A0005/6)熔断且音频与视频分表、拆帧硬解失败→软解(且**取消不记黑名单**)、合帧 NVDEC 的 8 帧探测(实测"20 帧命令永不结束")、硬件编码探测用**真实参数 + ≥5 帧 + 成品校验**(1 帧探测会让硬编被静默禁用)、硬编失败会话拉黑与"取消≠硬件坏"、重试闩锁的计数归还、引擎 30 秒无进展看门狗 + **管道读取 5 秒上限**、备用 ffmpeg 缺失告警。
43. **过度保守的一条已收紧**:"50 系没测过就直接当风险、走 ONNX" —— 它其实**早就名存实亡**(两个调用点都已显式关掉按型号预判),本轮把它改成"**只有旧引擎**才按型号预判",判据是可核对的引擎文件名(`…-2026.exe`,即指纹含 `VK_EXT_robustness2` 的那两份重编版)。以后新代码用默认参数调它也不会退回"50 系一刀切"。**说明:这是防回归改动,当前行为没变**(生产路径本来就以真机探测结论为准)。
44. **陈旧文案与判据说明已改准**:①自检报告里"RTX 50 系 ncnn 超分易黑帧"→ 改为"是否走 ncnn 由首次处理时真机实测决定"(重编版已在 50 系笔记本上实测通过);②"硬件编码失败 = 去掉 `-preset p4` 就能编"这条因果**已被实测否定**(真机制是 NVENC API 版本与驱动不匹配,去掉 preset 照样失败)→ 注释按实测改写;③NVDEC 探测注释写"30 秒硬超时"而代码是 6 秒 → 统一为 6 秒;④自检日志里"1×1 可用"其实跑的是 **320×240 活性检查** → 文案改准,并注明它只是活性检查、真正的可用性结论来自生产帧尺寸探测;⑤设置页"**无独显的电脑建议选 CPU**"已不成立(下拉早没有 CPU 项了)→ 改为"没有显卡时软件会自动改用 ONNX,再不行回退 CPU,不需要你手动选";⑥核显提示原说"建议勾选「兼容模式」",而该开关**只在视频处理页** → 已限定说明。
45. **已知还没改、留待办的两条**(都改行为、需真机验证,故不当场动):①**超分探测的缓存键里没有"模型"** → x4plus(实测 24.1 秒、最易出坏帧)可能被 animevideov3 的"通过"结论背书 7 天;修它要一并决定"一个任务会不会因此多探一次"。②**免探测快速通道的判据看的是"整机有没有 AMD/Intel 显卡",而不是"要用的那张卡"** → 结果绝大多数**混显笔记本**(Intel 核显 + N 卡)永远走不了快速通道,每个首次任务都要白等一次生产帧探测(最坏 60 秒)。两条都在 `SELF_AUDIT_20260912.md` 里有锚点与修法。

**【界面"卡死"的真正原因找到了并已修 —— 任务其实没卡】**
46. **"开了去重就在去重阶段卡死"的真正原因 = 日志区滚动条与自动换行互掐(已从根上修)**:诊断包实测(50 系笔记本 22:31)是 **`LayoutCycleException`(界面布局循环)**,而且**后台任务一直在正常跑**(日志显示补帧已推进到 171/285)—— 卡死的是**界面**,不是处理,你只能重启、把任务一起打断。根因很具体:日志区原来是"高度上限 220 + **滚动条按需出现**(Auto) + 文本**自动折行**"放在一个由内容决定高度的面板里 → 日志长过 220 时滚动条出现 → 内容宽度变窄 → 文本重新折行 → 高度又变了 → 滚动条又不需要了 → 宽度变回来 → 再折行…… 来回横跳 = 布局循环。**去重开着时日志行暴增、正好越过这个临界点**,所以只有开去重才崩。四条修法:
    - **根因修复:滚动条常驻**(内容宽度恒定 → 折行结果恒定 → 环路断掉)。视频页/图片页/抠图页/音频页的日志区**全部**改成常驻(这个隐患四页都有);
    - **不再给面板高度做动画**:展开/收起原来给"高度"做动画,而高度参与测量排布,会让下游每帧重算(另一个成环源)→ 改纯淡入淡出;
    - **"滚到底"推迟到下一帧**执行,不再在布局过程中改滚动位置;
    - **一次刷新只改一次日志文本**(原来是"先追加、再裁剪"改两次,每次都要重新测量)。
    另加**自愈兜底**:万一还有别的成因,一旦检出布局循环就**自动停用界面日志刷新**(日志继续写文件、任务照跑完),不会再出现"界面死了只能重启"。


> 📌 完整自检报告(已修 / 待办 / 文档与代码不一致)见仓库 **`SELF_AUDIT_20260912.md`**。其中有一条**已知未修**的高危项,如实告知:`v1.3.4` 公告第 5 条宣称"设备判定统一到 DeviceRouting",但该函数**生产代码里从未被调用**(只被单测调用),实际在跑的仍是三份重复解析 —— 也就是说**双卡机上"选独显却跑核显"的修复并未真正生效**。改它涉及多卡行为、需要多卡真机验证,故本轮只记录不动手。
