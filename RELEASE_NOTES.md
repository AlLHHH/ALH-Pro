# ALH Pro v1.1.0 更新说明

> 发布日期:2026-09-01
> 版本:v1.1.0(次版本)——新增重大能力 + 大量修复与改进

---

## ✅ 修复(Bug 修复)

1. **音频导出失败(致命)**:输出格式与编码档位错位(选 MP3 却用 WAV 编码)→ ffmpeg 报错、0 字节输出,即"根本不能导出";已修正,MP3/WAV/FLAC 全部正常。
2. **音频进度显示异常(585858 秒)**:ffmpeg `out_time_ms` 实为微秒被当毫秒;已按微秒换算并按总时长显示百分比。
3. **音频"增强"反而变差**:响亮归一会把已混音完成的音频压小 5dB+(音量变小、听感变差)——现在检测源已响(> -14dB)时自动跳过,只对真正音量小的文件归一。
4. **音频降噪无效**:原 anlmdn 滤镜在此 ffmpeg 构建零效果(实测无变化)→ 更换 afftdn(实测有效,降噪降 4dB)。
5. **音频导出降频**:响亮归一会把 44.1kHz 改成 48kHz → 滤镜链尾强制还原 44.1kHz。
6. **音频日志只显示一行**:改为追加式(时间戳 + 自动滚底 + 200 行截断)。
7. **按住查看原图松手变黑白**:抠图预览恢复逻辑无脑恢复蒙版;现精确恢复按住前的显示源。
8. **Real-ESRGAN 照片超分在 50 系/CPU 崩溃**:改用 ONNX 版(DirectML/CPU 都稳,不走 Vulkan)。
9. **视频超分 Real-ESRGAN 加载失败**:补 `-m models` 参数。
10. **rife-v4.26 + 高质量 TTA 卡死**:该模型 TTA 完全不可用,已禁用(服务端不传 + UI 置灰)。
11. **视频补帧级联少补**:非 2 的幂倍率(3x/12x)级联算错,已兜底回落最近 2 的幂。
12. **点"开始处理"无任何反馈**:各拦截点(选中为空/全部完成/超分补帧都关)现在写明原因到日志。
13. **CPU 模式崩溃/卡死时无提示**:降级时日志与进度明确"CPU 不可用,已自动改用 GPU N(显卡名)"。
14. **看门狗漏判":引擎启动即挂(50 系+旧 ncnn)原要等 8 分钟才降级 → 新增 30 秒启动超时。
15. **多引擎并发时看门狗失效**:时间戳私有化,不再互相"喂狗"。

## 🚀 新增

1. **音乐修复模型(Apollo)**:计划接入——把有损 MP3 压缩音乐重建回接近无损(清华/腾讯 AI Lab,CC BY-SA 4.0)。(本版预告,后续版本实现)
2. **Real-ESRGAN ONNX 版**:照片超分新链路(50 系/无独显/CPU 全兼容,BSD-3)。
3. **视频超分 ONNX 支持**:无独显用户视频超分也能用(ONNX CPU 稳)。
4. **智能自检线路选择**:处理前自检显卡,自动选择兼容引擎(50 系→ONNX/waifu2x,常规卡→ncnn GPU)。
5. **全设备兼容提示**:任何设备选了不兼容模型,黄字提示"建议改用 XX"(不限 50 系)。
6. **降级日志带显卡名**:每步降级显示具体 GPU 名称。
7. **关于弹窗「更新详情」**:可查看当前版本修复/改进/新增。
8. **特别致谢名单**:纯名字显示,自动扫描 thanks/ 文件夹。

## 🔧 改进

1. **音频 MP3 码率**:192k → 320k(源 256k 不再降档)。
2. **抠图默认 GPU**:DirectML 加速(DirectML 占满显卡会抖,已在设置保留"流畅模式"兜底)。
3. **音频功能默认全关**:降噪/低切/均衡默认关闭(不勾选不破坏),ToolTip 说明适用场景。
4. **发布合规**:THIRD_PARTY_NOTICES 完整(15 组件,含新增 ONNX/Apollo),关于页模型致谢 14 项。
5. **关于弹窗简化**:功能描述精简,致谢名单单层展示。

---

## 已知限制(诚实说明)

- 3 个图片超分引擎的 **CPU 模式**(waifu2x/realcugan/realesrgan)在部分设备崩溃——属引擎二进制问题(社区通病),软件已降级链兜底(自动改用 GPU)。
- **50 系 + Real-CUGAN(2022)**:无法用 GPU(官方停更),建议改用 waifu2x(50 系稳定)。
- **RIFE 权重**(补帧模型):许可仅为"个人/研究使用",免费发布没问题;若商业售卖需注意。
- **Apollo 音乐修复**:本版仅预告,未实现(需 OpenVINO 集成,见桌面计划文档)。

---

*感谢:1c0ay、小野寺小咲☁️、JNyxn、f1ower 帮忙测试反馈,让 ALH Pro 越来越好。*
*本软件免费公益,所有处理在本机完成,不上传不收集数据。*
