# 后处理 50 档全测(两个超分基底 × 每种后处理单独开)

素材与基底同 `REPORT.md`:真实 1920×1080 动画帧作参考,960×540 降质后超分回 1080p。
**所有后处理强度固定 50**,滤镜公式照 `VideoService.BuildPostFilter` 逐字复现:

| 后处理 | 强度 50 时的实际滤镜 |
|---|---|
| 锐化 | `unsharp=5:5:0.75:5:5:0` |
| 清晰 | `unsharp=9:9:0.40:9:9:0` |
| 钝化蒙版 | `smartblur=luma_radius=2:luma_strength=-0.50:luma_threshold=8` |
| 保留细节 | `cas=strength=0.50` |
| 去模糊 | `smartblur=luma_radius=3:luma_strength=-0.40:luma_threshold=2` |
| 边缘抗锯齿 | `sab=lr=1:ls=1.5` |

## 干净素材

| 后处理(50) | waifu2x n2 PSNR/SSIM/detail | animevideov3 PSNR/SSIM/detail |
|---|---|---|
| 不加(基准) | 49.66 / 0.9892 / 21.0 | 36.32 / 0.9698 / 67.6 |
| 锐化 | 42.67 / 0.9671 / 48.7 | 33.67 / 0.9275 / 171.2 |
| 清晰 | 43.83 / 0.9792 / 38.9 | 34.39 / 0.9223 / 129.3 |
| **钝化蒙版** | 42.48 / 0.9821 / 46.1 | 34.52 / **0.9782** / 115.0 |
| 保留细节 | 45.88 / 0.9671 / 30.5 | 35.98 / 0.9573 / 100.3 |
| 去模糊 | 47.05 / 0.9779 / 26.6 | 35.89 / 0.9441 / 82.9 |
| 边缘抗锯齿 | 48.80 / 0.9674 / 21.6 | 36.32 / 0.9761 / 66.0 |

## 压缩素材(h264 crf30)

| 后处理(50) | waifu2x n2 | animevideov3 |
|---|---|---|
| 不加(基准) | 36.23 / 0.9654 / 12.0 | 34.99 / 0.9599 / 54.1 |
| 锐化 | 35.69 / 0.9678 / 28.5 | 32.96 / 0.9357 / 135.8 |
| 清晰 | 35.83 / 0.9630 / 22.8 | 33.42 / 0.9239 / 103.9 |
| **钝化蒙版** | 35.80 / 0.9373 / 26.0 | 33.82 / **0.9605** / 88.0 |
| 保留细节 | 35.67 / 0.9432 / 18.3 | 34.57 / 0.9508 / 78.1 |
| **去模糊** | 36.18 / **0.9730** / 16.4 | 34.69 / 0.9314 / 68.2 |
| 边缘抗锯齿 | 36.27 / 0.9587 / 12.6 | 34.97 / 0.9200 / 53.1 |

## 结论

1. **每个后处理在 50 档都真的在干活**:锐化类把 detail 拉高 2~2.5 倍(21→48、67→171)。
2. **PSNR/SSIM 普遍下降是正常的**:这些指标衡量"像不像原图",而锐化本来就是要偏离原图;
   判断该看 detail + 肉眼看图,不能只看 PSNR。
3. **钝化蒙版是唯一的"双赢"**:在 Real-ESRGAN 输出上加 50 档,
   **SSIM 反而升**(干净 0.9698→0.9782、压缩 0.9599→0.9605),同时 detail +70%。
   → "想要更清晰"的最优解是:**基底用 Real-ESRGAN + 加钝化蒙版**,而不是换更大的模型。
4. **去模糊对 waifu2x 有用**(压缩源 SSIM 0.9654→0.9730,PSNR 基本不变):
   waifu2x 输出偏软,去模糊把它救回来一点。
5. **锐化 50 太狠**:PSNR −2~−7 dB、SSIM −0.02~−0.04,容易出白边 → 建议 20~30。
6. **边缘抗锯齿要分情况**:waifu2x 基座上几乎无损(PSNR −0.86、detail 不变),
   Real-ESRGAN 基座上明显伤 SSIM(压缩源 −0.04)→ 只在锯齿肉眼可见时开。

## 文件

- 对比图(桌面):`ALHPro_后处理对比_干净素材.png`、`ALHPro_后处理对比_压缩素材.png`
  (行 = 后处理,列 = 两个基底,3× 放大;左侧一列是标签)
- 复现:`run_post.py`(指标)、`make_post_sheets.py`(对比图)

注:这里测的是**视频页**的 6 种后处理。图片页还有「边缘增强(Sobel)」「减少杂色」等,不在本次范围内。

---

# 附:锯齿区实测(差异最大区域 120×80)+ 两个"非锐化"替代

上一节的数字用全帧平均,会稀释差异;这里把区域收窄到"加锐化后变化最大"的 120×80
(自动挑选:按 |锐化50 − 基底| 的均值最大),并加了两个非锐化方向的候选。

## 压缩素材 · amimevideov3 基底(基准 24.30 / 0.8539 / 1121.1 / 10.40)

| 后处理(50) | PSNR | SSIM | detail | edge |
|---|---|---|---|---|
| 基底(不加) | 24.30 | 0.8539 | 1121.1 | 10.40 |
| 锐化 | 21.28 | 0.7810 | **2787.4** | **13.30** |
| 清晰 | 22.02 | 0.8008 | 2058.7 | 12.58 |
| 钝化蒙版 | 23.27 | 0.8135 | 1332.0 | 12.15 |
| 保留细节 | 23.53 | 0.8358 | 1509.1 | 11.01 |
| 去模糊 | 24.26 | 0.8490 | 1138.8 | 10.78 |
| **边缘抗锯齿(现有)** | 24.36 | 0.8559 | **1098.0** | 10.32 |
| ★中值 3 | 23.71 | 0.8122 | 614.0 | 7.59 |
| ★轻度 smartblur | 24.38 | 0.8567 | 1129.4 | 10.32 |

## 干净素材 · waifu2x n2 基底(基准 41.07 / 0.9943 / 296.3 / 9.53)

| 后处理(50) | PSNR | SSIM | detail | edge |
|---|---|---|---|---|
| 基底(不加) | 41.07 | 0.9943 | 296.3 | 9.53 |
| 锐化 | 32.32 | 0.9740 | 655.8 | 11.02 |
| 钝化蒙版 | 32.81 | 0.9703 | 564.2 | 11.08 |
| 去模糊 | 39.96 | 0.9903 | 314.0 | 9.86 |
| **边缘抗锯齿(现有)** | 40.95 | 0.9936 | **296.3** | 9.50 |
| ★中值 3 | 28.00 | 0.9148 | 192.2 | 7.40 |
| ★轻度 smartblur | 40.68 | 0.9933 | 292.3 | 9.45 |

## 三条硬结论

1. **5 / 6 是同一个算子家族**:锐化、清晰、钝化蒙版、保留细节(cas)、去模糊 本质都是"反锐化"
   (unsharp / CAS),只有半径和阈值不同 —— 所以用户"感觉都像锐化"不是错觉。
   唯一反方向的是**边缘抗锯齿**(模糊)。
2. **边缘抗锯齿 50 实际上是空转**:在锯齿最密的区域,detail 只从 1121.1→1098.0(−2%)、
   SSIM 0.8539→0.8559(+0.002) —— 它既没削锯齿也没伤结构,等于没开。
   这就是"锯齿治不了"的结构性原因:**5 档只会加重锯齿,唯一该收尾的那档力度约等于 0**。
3. **中值滤波方向对、力度过猛**:detail −45%(1121→614)、edge −27%,确实在削锯齿,
   但 SSIM −0.04~−0.08、PSNR −0.6~−2.6 dB → 需要做成"只作用于强边缘阶梯"的受控版本,
   而不是全图median。轻度 smartblur 则过弱(≈无效)。

## 修复方向(待做)

1. **给锐化类加阈值**:现在锐化的 unsharp 阈值 = 0,等于全图锐化(连锯齿、噪点一起提);
   而带阈值 8 的钝化蒙版是唯一 SSIM 不下降的一档 —— 数据支持把阈值推广到其余几档。
2. **边缘抗锯齿重做**:提高 sab 强度或换成阈值化的方向性滤波(只打阶梯边缘),
   并用本报告同一套脚本验证"SSIM 不降 + 锯齿指标下降"两个条件同时成立。
3. **动漫素材锐化建议 20~30**:50 档在细节区 PSNR −4.7 dB、SSIM −0.08,且白边明显。

## 对比图

桌面:`ALHPro_后处理对比v2_压缩素材.png` / `ALHPro_后处理对比v2_干净素材.png`
(行 = 基底 + 各后处理 50 + 两个 ★替代;列 = 两个基底;4× NEAREST 像素放大;
区域按"差异最大"自动挑选 —— 上一版用 LANCZOS 放大 + 软边区域,把差异抹掉了,已作废并删除)

---

# 附二:重做后的正式公式(2026-09 全部重做,已进代码 + 预设)

旧的 6 档里 5 档是同一个 unsharp 的不同半径、边缘抗锯齿实测空转 → 全部重做。
**ffmpeg 的 `unsharp` 没有阈值参数**(第 6 个是色度强度),所以"带阈值保护"只能用 `smartblur` 负强度实现。

| 界面档 | 新公式(强度 v) | 机制 | v=50 实测(压缩素材/Real-ESRGAN 基底) |
|---|---|---|---|
| 锐化 | `smartblur=luma_radius=1:luma_strength=-v/100:luma_threshold=3` | 带阈值的细节反锐化 | PSNR 34.64 / edgePSNR 25.61 / detail 71.1 |
| 清晰 | `unsharp=13:13:min(0.5,v/100*0.5):13:13:0` | 大半径中调局部对比 | 33.78 / 24.33 / 88.6 |
| 钝化蒙版 | `smartblur=luma_radius=2:luma_strength=-v/100:luma_threshold=8` | 只锐化超过阈值的边缘 | 33.82 / 24.60 / **edgeSSIM 0.9582 ≥ 基底 0.9571** |
| 保留细节 | `cas=strength=min(0.6,v/100*0.6)` | 对比度自适应(设计上无白边) | 34.70 / 25.44 / **flatErr 2.09 ≈ 基底 2.07** |
| 边缘抗锯齿 | `sab=lr=1/2/3(按档):ls=2+v/100*2` | 形状自适应模糊(真的削锯齿) | 35.01 / **edgePSNR 26.04** / detail 50.2(较基底 −7%) |
| ~~去模糊~~ | **整项移除** | ffmpeg 无反卷积滤镜;卷积核方案实测 PSNR −8.6 dB | — |

基底(不加):PSNR 34.99 / SSIM 0.9599 / edgePSNR 25.99 / edgeSSIM 0.9571 / detail 54.1 / flatErr 2.07

**对比旧公式**:旧「锐化」(`unsharp=5:5:1.5:5:5:0`)在 v50 是 PSNR 31.22、**edgePSNR 21.13(边缘区 −4.86 dB)**、
detail 243 —— 这就是用户看到的"白边 + 锯齿加重"。新公式把边缘区损失从 −4.86 dB 压到 −0.38 dB。

**边缘抗锯齿的变化**(用户明确反馈"加了反而锯齿感更强、抗锯齿也没用"):
旧 `sab=lr=1:ls=1.5` → detail 54.1→53.1(**−2%,等于空转**);
新 `sab=lr=2:ls=3.0` → detail −7%、**SSIM 反升 0.0123、edgePSNR 反升 0.05** = 真的在削阶梯/振铃而不毁结构。

**同步更新**:
- 视频页 UI 删掉「去模糊」滑杆(其余五档 tooltip 全部改成如实描述机制与实测数据)。
- 图片页模式列表顺序与视频页统一为 **Real-ESRGAN 在上、waifu2x 在下**;
  存盘/预设沿用旧约定(0=waifu2x / 1=Real-ESRGAN),靠 `EngineFromStored/ToStored`(视频)、
  `ModeFromStored/ToStored`(图片)换算,老用户已存的选择不会被翻转。
- 内置预设同步:「通用画质增强 不含补帧」Rev 2、「动漫通用」Rev 3
  (钝化蒙版给最多 35/40,保留细节 40,边缘抗锯齿 45,去模糊归零,锐化收到 20)。
