# 图片页「画质增强」逐档钉准现状 + 逐档实测(2026-09-12)

> 上一轮把**视频页**的 6 档后处理全部重做(`84c3644`),但**图片页的 10 档从来没做过那次重做**
> (`POSTPROC_REDESIGN.md:59` 自己写了"图片页还有「边缘增强(Sobel)」「减少杂色」等,不在本次范围内")。
> 本文件就是给图片页补上那次"钉准现状 → 逐档实测 → 定参数"。
>
> 实测方式:**内核从 `EngineService.cs` 逐字生成**(`_qa\imgpost\extract_kernels.py` → `Kernels.g.cs`),
> 所以测的就是软件真正在执行的那份代码,不是重写版。

---

## 0. 先纠一个数:不是 11 档,是 10 档(+2 个开关)

| 位置 | 数量 | 明细 |
|---|---|---|
| 「增强」面板滑杆 | **10** | 去雾 / 减少杂色 / 锐化 / 清晰 / 钝化蒙版 / 保留细节 / 细节增强 / 去模糊 / 边缘增强 / 边缘抗锯齿 |
| 面板外的相关项 | 2 | 「预处理降噪」(弱/中/强,waifu2x cunet 前置)、「高级增强 TTA」(勾选框) |

`EnhanceImage` 里的**真实执行顺序**(`EngineService.cs`,按 `passes.Add` 的先后):

```
去雾 → 减少杂色 → 保留细节 → 细节增强 → 清晰 → 钝化蒙版 → 去模糊 → 边缘增强 → 锐化 → 边缘抗锯齿
```

于是"11 档"要么把「预处理降噪」算进来、要么把 TTA 算进来。**单独看增强面板就是 10 档。**

---

## 1. 逐档核对:注释 / 提示词 vs 实现(13 处)

### 1.1 交接说明点名的那一处:**在本轮 HEAD 上不成立**

> 交接说明:"已抓到一处:注释说'强度 51+ 再做一遍中值',但函数体里找不到对应代码"

`EngineService.ApplyMedianInMemory` 里**有**这段:

```csharp
MedianChannel(r, w, h); MedianChannel(g, w, h); MedianChannel(b, w, h);
if (strength > 50) { MedianChannel(r, w, h); MedianChannel(g, w, h); MedianChannel(b, w, h); }
```

`git log -S "if (strength > 50)"` 显示它**自首次提交 `2259534` 起就存在**,从未被删。
真正的注释与实现不符**另有一处**(见下表第 5 条:去模糊)。

### 1.2 真实清单

| # | 档位 | 注释/提示词怎么说 | 代码实际是什么 | 判定 |
|---|---|---|---|---|
| 1 | 减少杂色 | "强度 51+ 时再做一遍(更彻底)" | `if (strength > 50)` 后确实又跑一遍中值 | ✅ 一致(交接说明那条不成立) |
| 2 | 减少杂色 | UI 是 0~100 滑杆(101 个位置) | **只有两个状态**:1~50 跑一遍、51~100 跑两遍 | ❌ 档位与界面不符(见 3.4 实测) |
| 3 | 去雾 | "局部(15×15)最小值滤波" | 半径 2 跑 6 遍 = 有效半径 12,**25×25** | ❌ |
| 4 | 去雾 | "透射率 t = 1 − ω·dark/A" | 实际是 `t = 1 − 0.95·min(1, dark/229.5)` —— **大气光 A 根本没进 t**(A 只在后面减光幕时用);另 `darkNorm` 是死变量 | ❌ |
| 5 | **去模糊** | "并在最后与'轻微锐化'补一下边缘" | 函数体里**没有这一步**(只有 RL 迭代 + 写回截断) | ❌ **这才是那处注释不符** |
| 6 | 去模糊 | UI 提示"理查森-露西反卷积" | >900 万像素时**静默退化成 unsharp**(`ApplyUnsharpInMemory(strength/100*1.5,2,6)`),UI 未披露 | ❌ 未披露的降级 |
| 7 | 去模糊 | —(未提) | 核半径在**强度 60 处从 1 跳到 2**;迭代数在 ≥89 处封顶 → 90/100 输出完全相同 | ❌ 隐藏台阶(见 3.3) |
| 8 | 保留细节 | "CLAHE 风格局部对比度" | `原值 + (原值 − 7×7 盒均值) × k` —— **盒均值反锐化**,没有直方图/分块/裁剪限制,不是 CLAHE | ❌ 名字与机制不符 |
| 9 | 细节增强 | "原图 − **高斯**模糊" | 是 **3×3 盒均值** | ❌ 措辞 |
| 10 | 细节增强 | UI:"温和,不易放大噪点" | 半径 1 高通是**最**放大噪点的一种(高频 = 噪点) | ❌ 与原理/实测相反 |
| 11 | 边缘增强 | "沿梯度方向加一次差分" | 实际 = `mag×amount×0.25 + sign(拉普拉斯)×amount×8`:第二项只取拉普拉斯**符号**给固定偏置,既不是差分也不分方向 | ❌ |
| 12 | 边缘增强 | UI:"不放大噪点" | 梯度幅值与拉普拉斯符号在噪点上都不为 0 → 必然放大(实测 flatErr 2.60→5.08) | ❌ 与实测相反 |
| 13 | 增强总注释 | 顺序写成"减少杂色→保留细节→清晰→钝化蒙版→去模糊→边缘增强→锐化→边缘抗锯齿" | **漏了「去雾」和「细节增强」两项**,顺序也与代码实际不符 | ❌ |
| 14 | 边缘抗锯齿 | "平坦区域完全不动,细节不糊" | 前半对;后半过强 —— 阈值是"3×3 邻域最大差 ≥16",任何局部对比 ≥16 的纹理都被混 0.55 均值(实测 detail 54.1→23.1@100) | ⚠ 措辞过强 |

**本轮已把这 13 处注释/提示词按代码改正**(纯文字,行为未变 —— 唯一改行为的是「减少杂色」,见第 4 节)。

---

## 2. 实测方法

素材沿用 `_qa\ab_waifu` 的约定:真实 1920×1080 动画帧(`_qa\batchin\f_001.png`)作真值,
960×540 降质后超分回 1080p。**四种组合**都测:

| | 干净素材 | 压缩素材(h264 crf30) |
|---|---|---|
| waifu2x cunet n2(2x) | clean/w2x | comp/w2x |
| Real-ESRGAN animevideov3(2x) | clean/esr | comp/esr |

指标(白边/振铃是本轮新加的,因为"与真值的差"会把 GAN 模型天生的创造性偏离也算进去,不严谨):

- `PSNR/SSIM` 全帧;`edgePSNR/edgeSSIM` 只在参考图的强边缘像素上算(占 3.25%)
- **`over%`** = 输出比"参考图 3×3 邻域最大值"还亮 12 级以上 → **亮白边(过冲)**
- **`under%`** = 比邻域最小值还暗 12 级以上 → 暗边/振铃;`edgeOver%` 只统计强边缘像素
- `detail` 拉普拉斯方差(锐度代理);`flatErr` 平坦区 RMSE(噪点放大)

为公平起见另补三组针对性测试:**颗粒素材**(基底叠 σ=7 高斯颗粒)、**真雾素材**(按上浓下薄合成,最浓 0.75A)、
**1 像素细线靶标**(1080p,线宽 1/2/3px × 对比度 40/80/160 + 发丝式细曲线)。

---

## 3. 实测数据与发现

### 3.1 四组基底 + 各档强度 50(压缩素材 / Real-ESRGAN 基底为例)

| 档位(50) | PSNR | edgePSNR | over% | **edgeOver%** | detail |
|---|---|---|---|---|---|
| 基底(不加) | 34.99 | 23.19 | 0.073 | 0.83 | 54.1 |
| 减少杂色 | 35.23 | 23.54 | 0.060 | 0.60 | 48.3 |
| 保留细节 | 33.27 | 20.70 | 0.326 | 6.23 | 89.9 |
| 细节增强 | 34.19 | 21.83 | 0.132 | **2.34** | 85.0 |
| 清晰 | 31.93 | 19.11 | 0.671 | 12.08 | 124.8 |
| 钝化蒙版 | 32.14 | 19.05 | 0.530 | 11.72 | 138.8 |
| 去模糊 | 32.89 | 20.00 | 0.513 | 11.92 | 115.7 |
| 边缘增强 | 31.20 | 18.01 | 0.753 | **17.78** | 124.1 |
| 锐化 | 31.91 | 18.74 | 0.539 | 12.25 | 172.5 |
| **边缘抗锯齿** | **35.53** | **24.29** | 0.058 | **0.44** | 36.1 |
| ★预设 通用变清晰 | 29.52 | 16.07 | 1.126 | **23.62** | 238.0 |
| ★预设 清晰MAX | 28.54 | 15.10 | 1.728 | **33.95** | 245.1 |

完整四组组合、6 个强度档的原始表:`_qa\imgpost\imgpost_out.txt`;白边指标表:`imgpost_halo.txt`。

**「去雾」在无雾素材上是灾难**(同一组合):强度 10 已经 PSNR −11dB、SSIM 0.9654→0.8725;
强度 30 有 **37.8% 的像素被截断**;50 档 63%;100 档 100%。四组组合一致。

### 3.2 「哪些是纯亏」

| 档 | 判定 | 依据 |
|---|---|---|
| **边缘增强** | **纯亏,建议删或压到 0~15** | 十档里唯一"单边过冲"的算子:edgeOver 0.83%→17.78%@50→47.89%@100,而 under 始终 ≈0.05% —— 它只往亮的方向加(`sign(拉普拉斯)×8`),所以只出白边不出暗边。同时 PSNR −3.8dB@50、flatErr 2.60→3.38。而强度 10 档几乎不动(detail 54.1→54.7)= **整个可用区间基本是空的** |
| **去雾(无雾素材上)** | **纯亏** | 见 3.1。只在真有雾时有意义(见 3.5) |
| **去模糊(超分后的图上)** | **基本纯亏** | 强度 10 档 PSNR −1.58dB、edgeOver 0.83%→**9.22%**、detail 54→87;而超分后的图本身并不模糊,RL 反卷积没有模糊可回收。另有 60 档隐藏台阶与 >900 万像素静默降级 |
| **减少杂色(旧实现)** | **不是纯亏,但"假可调"** | 四组基底上 PSNR/edgePSNR 都是小幅**净收益**(详见 3.4)。真正的毛病是滑块只有两个状态 + 1px 细结构归零 |
| **两个官方预设** | **参数过猛** | 见 3.6 |

### 3.3 「哪些重叠」——用 Δ图相关系数量化

强度 50、同一基底,把每档输出与基底的差当向量算相关系数(`imgpost_overlap.txt`,四组组合一致):

| 档位对 | Δ 相关 |
|---|---|
| 保留细节 ↔ 清晰 | **0.984** |
| 保留细节 ↔ 钝化蒙版 | 0.952 |
| 锐化 ↔ 钝化蒙版 | 0.960 |
| 锐化 ↔ 保留细节 | 0.934 |
| 锐化 ↔ 细节增强 | 0.941 |
| 细节增强 ↔ 保留细节 | 0.834 |
| 减少杂色 ↔ 锐化 | −0.452 |
| 减少杂色 ↔ 边缘抗锯齿 | +0.380 |
| 去雾 ↔ 其它任意档 | ≈0(−0.05~0.02,完全正交) |

**结论:锐化 / 清晰 / 钝化蒙版 / 保留细节 / 细节增强 / 去模糊 这 6 档是同一个算子家族**(盒模糊 unsharp),
只有半径与阈值不同 —— 与视频页重做前的诊断**完全同型**(当时 6 档里 5 档是同一个 unsharp)。
唯一反方向的两档是「减少杂色」与「边缘抗锯齿」。

**在同样的细节增幅下谁最划算**(以 comp/esr 为例,基底 detail 54.1,取 detail≈85~100 的点):

| 档 | detail | PSNR | edgePSNR | edgeOver% |
|---|---|---|---|---|
| **细节增强 50** | 85.0 | **34.19(−0.80)** | **21.83(−1.36)** | **2.34** |
| 保留细节 50 | 89.9 | 33.27(−1.72) | 20.70(−2.49) | 6.23 |
| 钝化蒙版 30 | 101.0 | 33.22(−1.77) | 20.48(−2.71) | 6.22 |
| 清晰 30 | 94.4 | 33.12(−1.87) | 20.55(−2.64) | 6.34 |
| 锐化 30 | 119.3 | 33.09(−1.90) | 20.23(−2.96) | 6.46 |
| 去模糊 10 | 87.4 | 33.41(−1.58) | 20.73(−2.46) | 9.22 |

→ **「细节增强」在同等细节增幅下代价最小**(PSNR/边缘损失最小、白边最少),而它恰好是被 UI 提示
写成"温和,不易放大噪点"的那一档 —— 名字与提示都写错了方向:**它是锐化族里最值得用的那一档**。

### 3.4 「减少杂色」到底哪里坏了(直接决定本轮改动)

**① 交接说明里的数字复现不出来。** 交接说明写"实测 50 档就掉 3dB、细节只剩 41%"。
用图片页这一档自己的实测(压缩/Real-ESRGAN 基底,旧实现)。

| 强度 | PSNR(基底 34.99) | SSIM | edgePSNR | detail |
|---|---|---|---|---|
| 10 / 30 / 50(**逐像素完全相同**) | 35.23 (+0.25) | 0.9399 | 23.54 (+0.35) | 48.3 |
| 70 / 90 / 100(**逐像素完全相同**) | 35.27 (+0.28) | 0.9313 | 23.59 (+0.39) | 47.3 |

四组组合都是"小幅净收益":干净 waifu2x −0.63dB(状态一)/ −1.38dB(状态二)、
干净 Real-ESRGAN **+0.39/+0.42dB**、压缩 waifu2x ≈0、压缩 Real-ESRGAN +0.25/+0.28dB。
**"掉 3dB"不成立**;"细节只剩 41%"也不成立(全帧拉普拉斯方差只掉 3~18%)。

**② 真的毛病之一:滑块是假的。** 上面两张表:强度 10/30/50 的输出**逐像素完全相同**,70/90/100 也完全相同。
101 个位置只有 **2 个**不同结果 —— 用户想"少降一点"根本做不到,一开就是满力。
(而且旧的"状态二"在干净素材上是净亏:干净 waifu2x −1.38dB、edgePSNR −2.24dB。)

**③ 真的毛病之二:1 像素细结构 100% 消失。** 全帧方差看不出来,所以做了 1080p 细线靶标:

| 算子 | 1px 线(对比度 40) | 1px 线(160) | 2px | 3px | 发丝(1px 曲线) |
|---|---|---|---|---|---|
| **旧中值(任何强度)** | **0.0%** | **0.0%** | 100% | 100% | **10.4%** |
| 双边 σ=12 | 100% | 100% | 100% | 100% | 100% |
| 双边 σ=30 @100 | 50% | 100% | 72.5% | 84.2% | 100% |
| Sigma σ=12/20 | 100% | 100% | 100% | 100% | 100% |

**3×3 中值会把 1 像素宽的线/发丝整体抹掉(保留率 0%),但 2px 以上完全不受影响**
—— 这才是"细节只剩一点点"说法的真实来源方向:不是全帧细节掉一半,而是**最细的那一类结构直接归零**。

**④ 该不该把中值扔掉?不该。** 拿它和更"讲究"的候选(双边、Sigma)正面比过之后 —— 中值反而最好:

真实基底(没有额外加颗粒,ΔPSNR;各档取自己的最强档):

| 基底 | 旧中值 @25 | 旧中值 @75/100 | 双边 σ=30 @100 | 双边 σ=20 @100 | Sigma σ=20 @100 |
|---|---|---|---|---|---|
| comp/w2x | +0.02 | −0.00 | −0.09 | −0.07 | −0.14 |
| comp/esr | +0.24 | **+0.28** | +0.06 | −0.10 | −0.23 |
| clean/w2x | −0.63 | **−1.38** | −2.91 | −2.58 | −4.10 |
| clean/esr | +0.39 | **+0.42** | +0.29 | −0.07 | −0.33 |

带颗粒基底(σ=7,ΔPSNR):

| 基底 | 旧中值 @25 | 旧中值 @75/100 | 双边 σ=30 @100 | Sigma σ=20 @100 |
|---|---|---|---|---|
| comp/w2x | +2.69 | +3.01 | **+3.13** | +3.00 |
| comp/esr | +2.84 | **+3.23** | +3.00 | +2.69 |
| clean/w2x | +6.57 | +7.86 | **+8.63** | +7.90 |
| clean/esr | +3.81 | **+4.37** | +4.18 | +3.51 |

**4:2 平**(颗粒素材上双边赢两组、中值赢两组),但**真实素材上中值四组全赢**(干净 waifu2x 上 −1.38 vs −2.91 差距很大),
而且中值只要 9 个采样点、双边要 25 个(1080p 上约 2.7 倍耗时)。
→ **问题不在算子,在"力度不可调 + 最狠的那一端是唯一选项"。** 所以本轮是**改造映射,不是换算子**。

(完整表:`imgpost_grain.txt`、`imgpost_real.txt`、`imgpost_thinlines.txt`)

### 3.5 「去雾」的公平测试:它真的有用,但滑杆区间是错的

人工雾图(上浓下薄,最浓 0.75A,加 3×3 模糊),真值 = 未加雾的原图:

| 状态 | PSNR | SSIM | edgeBad% |
|---|---|---|---|
| 雾图(未处理) | 12.97 | 0.6741 | 83.46 |
| **去雾 48** | **21.68** | **0.8970** | **23.99** |
| 去雾 40 | 19.61 | 0.8437 | 36.13 |
| 去雾 52 | 20.46 | 0.8820 | 32.31 |
| 去雾 56 | 18.17 | 0.7703 | 56.22 |
| 去雾 70 | 11.52 | 0.3539 | 99.93 |
| 去雾 100 | 6.46 | 0.0070 | 99.93 |

**有效区间只有 40~52(峰值 48)**:<36 几乎无效(去雾 20 只把 PSNR 12.97→14.29),
≥56 开始截断(56 档 14.4%、60 档 23.8%、64 档 44.4%、70 档 61%、100 档 100%)。
**0~100 的滑杆里,能用的只有中间 1/8。** 建议把强度线性映射到 `amount = strength/100 × 0.55`
(整个滑杆压进有效区),并保留"无雾图不要开"的提示(本轮已加)。
代价要说清:这样等于把"最浓的雾也能硬去"的能力封在 0.55 —— 真遇到极浓的雾会不够力,
但按现在的映射,用户为了够力往上推的每一步都是在毁图(70 档已经把 61% 的像素推到 0)。

**本轮没有改去雾的算法**(只把强度重新定标,见 5.3),数据与建议都在这里。

### 3.6 两个官方内置预设**参数过猛**

| 组合 | 基底 edgeOver% | 通用变清晰 | 清晰MAX |
|---|---|---|---|
| comp/esr | 0.83 | **23.62** | **33.95** |
| clean/esr | —(over 0.024%) | edgeBad 5.40%→**70.58%** | →**73.99%** |
| comp/w2x | 0.13 | 6.98 | 14.83 |
| clean/w2x | 0.00 | edgeBad 0%→42.95% | →54.93% |

也就是说:**用户点一下官方预设,强边缘上出现过冲的像素从不到 1% 变成 23~34%**(压缩素材)/ 70%+ (干净素材),
PSNR 掉 5~7dB。**这就是"白边"的来源**。参数里有 `去模糊 35`(3.2 判定基本纯亏)、
`边缘增强 5/30`(判定纯亏)、`锐化 10/20`、`清晰 15/25`、`钝化蒙版 20/30`、`边缘抗锯齿 40/65`。

**✅ 已在同一轮内按实测重做**(见 5.3)。重做后四组基底的实测:

| 基底 | 基底 edgeOver% | 通用变清晰(新) | 清晰MAX(新) |
|---|---|---|---|
| comp/w2x | 0.13 | 0.42 | 0.63 |
| comp/esr | 0.83 | **4.33** | **5.84** |
| clean/w2x | 0.00 | 0.04 | 0.34 |
| clean/esr | 0.32 | 4.46 | 7.60 |

| 基底 | 旧 PSNR 损失 | 新 PSNR 损失 | 旧 detail | 新 detail |
|---|---|---|---|---|
| comp/w2x | −3.79 / −4.44 dB | **−0.28 / −0.38 dB** | 613% / 755% | **166% / 179%** |
| comp/esr | −5.72 / −6.37 dB | **−1.30 / −1.62 dB** | 476% / 505% | **169% / 176%** |
| clean/w2x | −16.6 / −18.2 dB | **−4.95 / −6.16 dB** | 685% / 857% | **171% / 187%** |
| clean/esr | −7.37 / −8.43 dB | **−1.58 / −1.97 dB** | 518% / 590% | **164% / 172%** |

过冲像素降到原来的 1/5,清晰度仍保持在基底的 **1.6~1.9 倍**。完整对照:`_qa\imgpost\imgpost_presets.txt`。

---

## 4. 「参数怎么定」——逐档建议(基于以上数据)

> 标注 ✅ 的已在本轮落地(见 5.3);其余仍是建议。

| 档位 | 建议区间 | 理由 |
|---|---|---|
| 去雾 | ✅ 已把映射压成 `amount = strength/100×0.55`(现在整段滑杆单调有效,峰值约在强度 87) | 3.5:<36 无效、>56 毁图 |
| 减少杂色 | ✅ 已改连续力度;**20~50**(轻抹颗粒),要"彻底"再上 70+ | 5.1:50 档四组基底都不亏 |
| 保留细节 | **20~40** | @50 已 −1.7dB / edgeOver 6.2% |
| **细节增强** | ✅ 已作为两个内置预设的主力;**30~50** | 3.3:同等细节增幅下代价最小 |
| 清晰 | **10~30** | @50 −3.1dB / edgeOver 12.1% |
| 钝化蒙版 | **10~30** | @50 −2.9dB / edgeOver 11.7% |
| 去模糊 | **建议默认 0**;真要用于"原片本身就糊"的图 | 3.2:超分后的图上基本纯亏 |
| 边缘增强 | **建议默认 0,最多 15** | 3.2:单边过冲,唯一纯亏档 |
| 锐化 | **10~25** | @30 −1.9dB / edgeOver 6.5% |
| 边缘抗锯齿 | **Real-ESRGAN 基底 40~60;waifu2x 基底 ≤30** | 十档里唯一"各项指标都变好"的一档,代价是 detail 直线下降;waifu2x 输出本来就软,再磨就没细节了 |
| 官方预设 | ✅ 已重做为「只留 细节增强 + 钝化蒙版 + 边缘抗锯齿」,`去模糊/边缘增强/锐化/清晰/保留细节/去雾` 全部归零 | 3.6 / 5.4 |
| 组合原则 | **锐化族最多开 1~2 档**(细节增强 + 钝化蒙版);重复叠同族只会叠白边 | 3.3 相关性 0.83~0.98 |
| 顺序 | 降噪 → 锐化族 → 边缘抗锯齿(现状顺序已符合) | 代码执行顺序 |

---

## 5. 本轮实际改动

### 5.1 唯一的行为改动:「减少杂色」从"两档开关"改成"连续力度"

```csharp
// 旧:一遍中值(1~50)/ 两遍中值(51~100) —— 101 个位置只有 2 个结果
// 新:目标 = 两遍中值,再与原图按强度线性混合  out = orig + (strength/100) × (target − orig)
```

| 强度 | comp/w2x ΔPSNR | comp/esr | clean/w2x | clean/esr | 1px 细线保留 |
|---|---|---|---|---|---|
| 10 | +0.01 | +0.04 | +0.08 | +0.07 | 90% |
| 20 | +0.01 | +0.08 | +0.11 | +0.14 | 80% |
| 25 | +0.01 | +0.10 | +0.11 | +0.18 | 75% |
| 50 | +0.02 | +0.19 | −0.13 | +0.32 | 50% |
| 75 | +0.01 | +0.26 | −0.66 | +0.42 | 25% |
| 100 | −0.00 | +0.28 | −1.38 | +0.42 | 0% |

**关键:强度 100 与旧的 51+ 档逐像素一致**(干净 waifu2x −1.38、干净 Real-ESRGAN +0.42 完全对上);
而 10~50 是**旧版本根本给不了的"轻力度"**(旧版在干净 waifu2x 上任何设置都是 −0.63dB,新版 20~25 是 **+0.11dB**)。
带颗粒素材上 ΔPSNR 从 +0.42 单调升到 +7.86dB。
验收脚本 `verify_ship.py` 跑的是**从改后的 `EngineService.cs` 重新生成的内核**(结果 `imgpost_ship_verify.txt`)。

**代价(实测,1920×1080 单张)**:新实现**总是**先算出"两遍中值"这个目标再混合,
所以低强度也比旧版贵:旧·≤50(一遍中值)0.57 s → 新 0.81~0.90 s(**1.57×**,约 +0.3 s/张);
旧·51+(两遍中值)0.84 s → 新同价(1.07×)。高强度不变,低强度多花 0.3 秒 —— 换来的是"可调"。

### 5.2 同步改动

- `UpscaleView.xaml` 的 6 条 tooltip 按实测改正(去雾只在有雾时开/减少杂色力度连续+1px 细线警告/
  细节增强才是锐化族首选/去模糊基本纯亏+900 万像素降级/边缘增强单边过冲/边缘抗锯齿建议区间)。
- 两个官方内置预设 `Rev 2 → 3`:旧基线里的 `减少杂色 20 / 35` 在新映射下只剩 0.20/0.35 的力度
  (= 把降噪悄悄关掉),按实测等效值改成 **50**。Rev 机制会只覆盖官方预设,自建预设一律不碰。

### 5.3 第二批(同一轮,在"钉准现状"结论出来后按实测动手)

**① 「去雾」强度重新定标**:`amount = strength/100` → `amount = strength/100 × 0.55`。
出厂代码实测(人工雾图):强度 10→13.11、30→13.88、50→15.65、70→19.02、**87→21.64(峰)**、100→18.77;
对比旧映射峰值 21.68**完全相同**,但旧映射拉满只有 6.46(整幅压黑)。**整段滑杆从"8/9 是废的"变成单调有效**,
代价是最浓的雾不如原来"够力"——原来那个"够力"是拿毁图换的。无雾素材上仍然有害(强度 25 就 −5.15dB),
所以提示保留"没雾不要开"。

**② 「边缘增强」强度限幅**:`amount = strength/100 × 0.8` → `× 0.25`。
出厂代码实测(强度 100):白边像素占比 comp/esr 从 **47.89%→8.76%**、clean/esr 从 71.58%→13.18%,
PSNR 从 27.58→32.99(**+5.4dB**)。它仍然是十档里最差的一档(单边过冲的机理没变),但"手一滑就一片白边"没有了。
**没有删这一档**:删控件会让老预设里已存的 Edge 值失去意义,这里选择限幅而非删除。

**③ 两个官方内置预设按实测重做**(`Rev 3 → 4`):老基线一口气开了锐化族 6 档里的 5 档
(锐化 10/20、清晰 15/25、钝化蒙版 20/30、保留细节 40/50、细节增强 10/20)+ 去模糊 35 + 边缘增强 5/30;
新基线**只留 2 档**(细节增强 50/60 + 钝化蒙版 20/30)+ 边缘抗锯齿 30/50,其余归零。
四组基底实测见 3.6:过冲像素降到 1/5、PSNR 损失从 −3.8~−18.2dB 收到 −0.3~−6.2dB,细节仍保持 1.6~1.9 倍。
启动时自动生效(`diagnostic.log` 16:39:47 两条"已更新到新基线(Rev 4)")。

**④ 「老预设效果变了」的告知方式改为写在更新公告里**,不单独弹窗(用户要求)。
`RELEASE_NOTES.md` 的 v1.3.4 一节新增 16~22 共 7 条,弹窗代码与配套的 `EnhanceRev` 字段、`ImgPresetNoticeRev`
标记已全部删除(无残留)。

### 5.4 第三批:锐化族 6 档「各自换机理」(不合并,而是让每一档真的不同)

用户明确要求:**保留 6 档、优化而不是合并**。做法与视频页当年那次重做同型 —— 每档换一个本质不同的机理。

| 档位 | 旧机理 | 新机理 |
|---|---|---|
| 锐化 | 逐通道、全图、无阈值的 USM(与钝化蒙版相关 0.98) | **亮度域**小核高通 + **按局部梯度加权**(强边缘多给、平坦少给)→ 不出彩色描边、白边减半 |
| 清晰 | 8 遍模糊的大核 USM,全亮度范围 | 同样大核,但**只作用中间调**(权重 1−(\|亮度−128\|/140)²) |
| 钝化蒙版 | 4 遍模糊 + 阈值 8 | **保持不变**(经典做法,当标准参照) |
| 保留细节 | 7×7 盒均值反锐化(自称 CLAHE) | **真 CLAHE**:8×8 分块直方图 + 限幅 + 双线性插值 |
| 细节增强 | 3×3 高通、无门槛 | 3×3 高通 + **软门槛**(幅值 ≤4 的压掉 = 噪声不放大) |
| 去模糊 | RL 反卷积,强度 60 处核半径 1→2 跳变 | RL + **连续核**(σ = 0.6+强度/100×1.6,支撑半径固定 3) |

**① 重叠度(核心指标,改动图两两相关;四组基底一致)**

| | 旧 | 新 |
|---|---|---|
| 六档两两相关 **均值** | 0.809 | **0.505** |
| 六档两两相关 **最大** | 0.984 | 0.941(锐化↔钝化蒙版,本质都是锐化,已明显减轻) |
| 保留细节 ↔ 其它五档 | 0.83~0.98 | **−0.05 ~ −0.02(完全正交)** |
| 去模糊 ↔ 其它 | 0.62~0.86 | 0.41~0.79 |

**② 单档质量(出厂代码,压缩/Real-ESRGAN 基底,强度 50;over% = 亮白边像素占比,基底 0.073%)**

| 档位 | PSNR | edgePSNR | over% | detail% |
|---|---|---|---|---|
| 锐化 | −2.03 | 19.94 | 0.315 | 236% |
| 清晰 | −3.41 | 18.69 | 0.865 | 267% |
| 钝化蒙版 | −2.84 | 19.05 | 0.530 | 256% |
| **保留细节** | **−0.31** | **23.34** | **0.101** | 99% |
| 细节增强 | −1.85 | 20.16 | 0.235 | 267% |
| 去模糊 | −3.10 | 19.26 | 1.049 | 164% |

**保留细节(CLAHE)是唯一天生不出白边的一档**:干净 waifu2x 基底上 over% 全程 **0.000~0.001**、edgePSNR 38.37→**38.33(几乎无损)**。
代价要说清:**detail 指标看不见它**(99%~101%)—— CLAHE 提的是"局部明暗层次",不是拉普拉斯方差;
这也是它和"反锐化"最本质的区别。要判断它好不好看得看图,不能只看表。

**③ 耗时(1080p 单张,强度 100)**:保留细节 0.22s、细节增强 0.22s、锐化 0.21s、钝化蒙版 0.26s、清晰 0.34s、去模糊 3.75s、减少杂色 0.68s。
换成 CLAHE 之后保留细节反而**更快**了(旧的 7×7 邻域循环是每像素 49 次采样)。

**④ 内置预设不用再改**:它们用的三个档(细节增强 50/60 + 钝化蒙版 20/30 + 边缘抗锯齿 30/50)在新算子下重测
仍是 detail 145%~160%、over% ≤0.22%,落在目标区间内,故保持 `Rev 4` 不动(避免又一次无实测依据的 Rev 提升)。

**⑤ 工具**:`measure_sharpen.py`(单档质量 + 重叠度矩阵)、`imgpost_sharpen.txt`、`imgpost_sharpen_overlap.txt`、`time_tiers.py`。

### 5.5 第四批:两个"最差档"重做机理(用户决定:**不删,只优化**)

| 档位 | 旧机理 | 新机理 | 实测(强度 100) |
|---|---|---|---|
| 边缘增强 | 梯度幅值×0.25 + **拉普拉斯符号**×固定偏置 —— 两项都只加正值,所以**只出白边不出暗边** | **对称拉普拉斯**(8×中心−邻域和,再 /8)+ 边缘加权 | 干净 waifu2x 基底 PSNR −10.99→**−3.65dB**、白边像素 0.027%→**0.002%**、细节 156%→**172%**;干净 Real-ESRGAN 基底强边缘白边 13.18%→**4.14%**、细节 138%→**196%**。over/under 由单边(0.464/0.040)变平衡(0.145/0.215) |
| 去模糊 | RL 反卷积,无任何约束;迭代最多 10 次 | RL + **逐迭代过冲钳制**(夹回观测图 3×3 局部范围 ±6 级)+ 迭代 2~6 次 | 干净 Real-ESRGAN 基底白边 **2.211%→0.552%**、PSNR 损失 **−5.10→−2.50dB**;干净 waifu2x 基底白边 1.408%→**0.004%**;1080p 单帧也更快(迭代少一半) |

两档**都保留**(删控件会让老预设里已存的 Edge/Deblur 值失去意义)。数据:`_qa\imgpost\imgpost_edgedeblur.txt`。
**注意强度语义变了**:老预设里这两个值的效果与老版不同(见更新公告第 17 条的提醒)。

### 5.6 仍然没做

- 4K 下其余后处理滤镜(smartblur / unsharp 13×13 / cas)仍有成本 —— 但现在日志有「编码阶段拆分」,能量出滤镜与编码各占多少,再决定要不要优化。
- 图片页「预处理降噪」「TTA」未实测(它们是处理前的另一条路径,不是后处理档)。

---

## 6. 复现方式

```powershell
cd D:\deep\alh-pro\_qa\imgpost
python extract_kernels.py      # 从 EngineService.cs 逐字生成 Kernels.g.cs(改了软件必须重跑)
python measure.py              # 四组基底 × 10 档 × 6 强度(约 10 分钟)
python measure2.py --grain     # 颗粒素材上的降噪候选对比
python measure2.py --haze      # 人工雾图上的去雾强度扫描
python measure3.py             # 白边/振铃指标(从已存 PNG 重算,不重跑算子)
python measure4.py             # 1080p 细线靶标:1px 结构保留率
python measure5.py             # 候选算子 × 真实基底(没噪点时会不会白亏)
python verify_ship.py          # ★ 出厂代码验收(改完必跑)
python make_sheets.py          # 桌面对比图
python make_gif.py             # 降噪时间维 GIF + 时间轴图
python verify_preset_notice.py # ★ 老预设换代提示的运行时验收(会备份并还原用户设置)
# 诊断包 force 实测路径验收(反射调用已部署的 ALHPro.dll)
cd ..\probetest; dotnet build -c Release; .\bin\Release\net8.0-windows10.0.19041.0\win-x64\ProbeTest.exe
```

输出:本目录下 `imgpost_*.txt`,对比图在桌面。

### 本轮产出的桌面文件

| 文件 | 内容 |
|---|---|
| `ALHPro_图片后处理_逐档总览.png` | 13 行(基底×2 + 10 档 + 2 预设)× 3 取样区(发丝/最平坦/眼睛) |
| `ALHPro_图片后处理_减少杂色_新旧对比.png` | 旧两状态 vs 新连续四点;右列是 1px 细线靶标(旧实现整列线条消失) |
| `ALHPro_图片后处理_去雾_有雾vs无雾.png` | 真人雾图上的强度扫描 + 无雾素材上开去雾的对照 |
| `ALHPro_降噪动态对比_时间维.gif` | 视频降噪四设置并排动图(48 帧) |
| `ALHPro_降噪时间轴对比图.png` | 时间波动放大图 + 时间轴 + 数字 |

---

## 7. 本轮验证到什么程度(以及没验证到的)

### 已验证(有运行证据)

| 项 | 证据 |
|---|---|
| 构建 0 错误 / 22 警告(与基线一致) | `dotnet build ImgUpscalerUI -c Release -p:Platform=x64` |
| 测试 275/275 | `dotnet test AlhPro.Tests -c Release` |
| 部署 + 启动 | `deploy.ps1` → "✅ 启动正常,窗口标题: ALH Pro v1.3.4" |
| 「减少杂色」新实现 | `verify_ship.py`:内核从**改后**的 `EngineService.cs` 重新生成,四组基底 × 6 强度的 ΔPSNR/1px 保留率全部实测(见 5.1) |
| 官方预设 Rev 2→3 真的生效 | `diagnostic.log` 16:09:05:已把两个官方图片预设更新到新基线(Rev 3);`img-presets.json` 里 `OfficialRev=3`、`Denoise=50` |
| 老预设提示·**反向路径** | 同一条日志:`[图片预设] 无需换代提示:没有旧基线(EnhanceRev<1)的自定义预设;已记标记`(用户当时只有官方预设) |
| 老预设提示·**正向路径** | `verify_preset_notice.py` 造 2 个换代前自定义预设 + 1 个本代自定义 + 2 个官方 → 启动后日志:`[图片预设] 换代提示:2 个自定义预设是旧一代后处理参数(EnhanceRev<1)保存的,已弹出提示`;官方与本代自定义**未被计入** |
| 老预设提示**只读**保证 | 同一次验收:预设文件启动前后 **SHA256 完全一致**(`8d22e55251cf487f`);验收脚本用完已还原用户原设置 |
| 诊断包 `force` 实测路径 | `_qa\probetest`(反射调用**已部署的** `发布版\ALHPro.dll`):`EnsureNcnnProbeAsync(engine,gpu,null,ct,force)` 签名确认存在;执行后 `ncnn-probe.txt` 从"只有 rife 一条"变成多了 `realesrgan2026\|0` 与 `waifu2x\|0` 两条真实结论;`DescribeNcnnVerdicts(gpu)` 返回真实路由而不是"未测" |

### 没验证到(如实说明)

1. **诊断包 zip 里"确实有 ncnn-probe.txt"这一步没跑通**。导出按钮在 GUI 里、且后面跟着系统保存对话框,
   本会话无法在无人值守下点它。已验证的是:①强制实测会产生真实结论并落盘(上表);
   ②打包清单里的那一行是 `File.Exists(probe) → File.Copy`(仅当文件存在时拷入临时目录,临时目录整体入 zip)。
   **要真正确认,请在软件里点一次「设置 → 导出诊断包」,看 zip 根目录里是否有 `ncnn-probe.txt`。**
2. **主观观感没做**。全部结论基于客观指标 + 放大截图;最终"好不好看"要人眼定。
   `SSIM` 在合成锐化下本身不单调(会因局部结构增强而回升),所以判定一律以 `PSNR/edgePSNR/over%/detail` 为主。
3. **真雾素材只有人工合成的一种**(浓度分布单一)。有效区间 40~52 会随真实雾浓度漂移,
   但"<36 无效、>56 崩"这个形状是可复现的。
4. **颗粒测试用的是 σ=7 高斯噪点**,不是真实的 h264 压缩噪声;真实噪声同时含空间与时间成分。
5. **只测了一种片源**(单张真实动画帧 + 一段 One Piece 演示片段)。不同画风(写实照片、文字界面、
   线条更细的番)结论可能不同,但"6 档同族""1px 结构被中值抹掉""边缘增强单边过冲"这三条是算子的固有性质,不依赖素材。
6. **TTA(高级增强)和「预处理降噪」不在本次实测范围**(它们是"处理前的另一条路径",不是后处理档)。
7. **降噪 GIF 的时间维收益被放大了**:素材里叠的是**纯时间维**噪点(`ffmpeg noise=allf=t`),
   所以"结合模式抖动 −95%"这个数字比真实片源漂亮。真实片源的噪点同时含空间与时间成分,
   收益会小一些;GIF 的作用是**让时间维看得见**(单帧上四个设置几乎一模一样)。
