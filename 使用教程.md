# ALH Pro 使用教程

**版本:v0.5 · 本地图片超分 / AI 抠图 / 视频超分补帧一体化工具**

---

## 一、软件简介

ALH Pro 是一款**完全本地运行**的图片与视频 AI 处理工具,包含三大功能:

| 功能 | 说明 |
|---|---|
| 图片放大 | AI 超分:动漫插画、真实照片无损放大 1x~4x |
| AI 抠图 | 一键移除背景,支持 6 款模型与精细边缘调整 |
| 视频处理 | 视频超分 / 视频补帧 / 去除重复帧,一站式批量处理 |

**隐私承诺:所有处理均在本机完成,文件不会上传到任何服务器。**

**系统要求:Windows 10 以上。**

---

## 二、快速开始(30 秒上手)

1. 启动 ALH Pro,左侧选择功能页(图片放大 / AI 抠图 / 视频处理);
2. 拖入或选择文件;
3. 保持默认参数,点击「开始处理」;
4. 完成后在输出目录查看结果。

软件启动时会自动检测本机 GPU:**有独立显卡自动启用 GPU 加速;无 GPU 或显卡不兼容时自动切换 CPU 模式**,无需手动配置,任何设备均可使用。

---

## 三、图片放大(图片超分)

### 3.1 算法选择(第一步,最重要)

| 算法 | 适用素材 | 说明 |
|---|---|---|
| **动漫插画 (waifu2x)** | 动漫、插画、二次元图片、游戏截图 | 对线条和色块友好 |
| **真实照片 (Real-ESRGAN)** | 照片、风景、人像等写实画面 | 对纹理和细节友好 |

选对算法,效果差异很大。**同一次处理只对所选算法生效,切换算法后重新添加图片即可。**

### 3.2 waifu2x 模型(动漫模式)

- **cunet(通用)**:画质优先,默认推荐;
- **upconv_7_anime(动漫插画)**:速度更快,适合漫画、简笔插画;
- **Real-CUGAN pro / se / nose**:专业级增强(pro 优先画质、se 语义增强、nose 无降噪),按需选择。

### 3.3 放大倍数

- **不放大 (1x)**:仅降噪,不改变尺寸(适合照片去噪);
- **1x 超分(2x 放大后缩回)**:AI 画质提升但保持原尺寸,推荐用于"只想变清晰"的场景;
- **2x / 3x / 4x**:直接放大 N 倍。**倍率由模型权重决定,置灰的倍率表示当前模型不支持。**

### 3.4 降噪与预处理

- **降噪级别**(动漫模式):不降噪 / 弱 / 中 / 强——JPEG 压缩痕迹明显时建议「中」;
- **预处理降噪**(照片模式专属):用 waifu2x 先降噪再超分,适合噪点多的照片(Real-ESRGAN 本身不降噪)。

### 3.5 高级(后处理增强)

按需勾选:去雾、减少杂色、锐化、清晰、钝化蒙版、保留细节、细节增强、去模糊、边缘增强、边缘抗锯齿。
**建议:大多数素材默认即可;效果不满意时,先尝试「保留细节」和「去模糊」,一次只加一项,避免过度处理产生噪点。**

### 3.6 输出

- **格式**:JPG(默认,文件小、通用)或 PNG(无损,透明背景用 PNG);
- **质量**:JPG 质量 1~100,默认推荐区间 85~95,数值越高文件越大;
- **输出目录**:默认与源图同目录;多张图片时自动在输出目录创建子文件夹;
- **支持格式**:PNG / JPG / WebP / BMP,支持多张批量、拖入即处理。

---

## 四、AI 抠图

### 4.1 模型选择

| 模型 | 特点 | 适合 |
|---|---|---|
| **U²-Net 通用** | 均衡稳定,速度较快 | 大多数普通图片 |
| **U²-Net 轻量** | 速度最快,精度略降 | 快速批量抠图 |
| **ISNet 精细边缘** | 边缘细腻,头发丝表现好 | 人像、产品图 |
| **ISNet 动漫** | 专为动漫优化 | 动漫、插画 |
| **BiRefNet 高精度(推荐)** | 综合精度最高 | 一般用途首选 |
| **BiRefNet 完整版** | 复杂背景、多主体 | 复杂场景抠图 |

### 4.2 预处理

- **降噪强度**:素材噪点明显时开启弱/中/强,先降噪再抠图,边缘更干净。

### 4.3 参数微调

- **前景阈值 / 背景阈值**:默认 128 / 64,一般无需修改;
- **边缘羽化**:数值越大边缘过渡越柔和;
- **边缘增强**:挖细毛、半透明边缘时适当提高;
- **边缘清理**:消除边缘杂质和毛刺。

### 4.4 预览与结果

- 处理完成后可**按住查看原图**对比效果;
- 支持 AI 彩色预览(原图 × 蒙版,棋盘格表示透明区域),所见即所得;
- 多张图片自动按输出目录分文件夹保存。

---

## 五、视频处理

### 5.1 支持与限制

- 支持格式:MP4 / MKV / MOV / WebM,支持批量;
- 支持多视频分别裁剪(拖动时间线)、分别设置参数;
- **至少启用一项处理(超分或补帧)才能开始**。

### 5.2 视频超分

| 超分引擎 | 适用素材 |
|---|---|
| **动漫 (Real-CUGAN)** | 动漫、动画视频 |
| **通用 (Real-ESRGAN)** | 真人、实拍、综合 |
| **经典动漫 (waifu2x)** | 老动画、低清动漫 |

- 放大倍数:1x 超分(2x 后缩回,变清晰不变尺寸)/ 1.5x / 2x / 3x / 4x;
- 也可以选**自定义分辨率**,按宽高精确输出;
- **速度提示:1x~2x 较快;倍率越高越慢、显存占用越大。**

### 5.3 视频降噪与后处理

- **视频降噪**:ffmpeg 非局部均值降噪,去视频噪点/闪烁/压缩噪点,比 AI 逐帧快很多;
- **视频调整(后处理)**:锐化、清晰、钝化蒙版、保留细节、去模糊、去频闪、去杂色、边缘抗锯齿——按需勾选,默认即可。

### 5.4 补帧 (RIFE)

**补帧 = 在原帧之间生成中间帧,让画面更流畅(动画、电影感)。**

- **补帧模型**:通用画质最新 (v4.13,推荐) / v4.6 / v4.26 / 动漫专用 / 高清 / 超高清 / 经典兼容 (v2.3);
- **帧率设置**(点击「视频帧率」展开):
  - **各视频默认帧率(推荐)**:每个视频按自身原始帧率处理,自动探测,无需设置;
  - **帧率偏移**:批量统一把帧率减 N(如 60fps 统一降为 30fps);
  - **单独调整**:逐个视频指定帧率;
- **输出倍率**:补帧后输出帧率 = 原帧率 × 倍率(如 30fps × 4 = 120fps)。

### 5.5 去除重复帧(去重,动漫必修)

**去重 = 删掉静态/低变化的画面(如动画中一张画停 3 帧),只保留真实内容帧。** 适合动漫、视频教程、静止画面多的素材,能大幅减小文件、方便补帧。

| 模式 | 原理 | 推荐场景 |
|---|---|---|
| **智能检测(默认)** | 自动估计素材节奏(一拍 N),按它均匀采样+补帧;估计不可靠自动回退保守策略,不硬猜 | 通用首选,无需调参 |
| **动漫模式** | 按动画拍型删保持帧(一拍二/一拍三/混合/半拍二/一拍四) | 明确知道是"拍二"风格的动画 |
| **手动模式** | 自选算法+自由调参 | 高级用户 |

- **智能策略**:均衡(推荐)/ 激进(删得多)/ 保守(几乎不误删);
- **相位自动对齐**:开自动估计保持帧相位,避免抽到中间帧,推荐保持开启;
- 手动模式算法:内容帧率采样(按你填的内容帧率均匀采样)/ 帧差+SSIM(最严谨)/ 重复帧检测 mpdecimate / 画面变化阈值 scene。

**去重后输出规则:输出帧率 = 内容帧率 × 补帧倍率;时长 = 源时长(尾帧恒保留)。**

### 5.6 果冻修复与光流调整(进阶)

- **减少果冻 / 运动模糊**:修复快速运动时的果冻感、运动模糊;
- **光流调整(时间步)**:补帧采样时间点,默认 0.50,一般无需修改。

### 5.7 输出

- **码率**:自动(推荐)/ 低 / 中 / 高 / 极高 / 自定义(Mbps)。1080p 常见清晰码率约 8Mbps;
- **封装格式**:MP4(通用,推荐)/ MKV;
- **编码格式**:H.264(所有设备都能播,推荐)/ H.265(同画质文件小一半,老设备可能不支持);
- **静音**:输出不含音轨(只要画面时勾选);
- **兼容模式**:低配置/无独显设备自动降低显存占用、防止爆显存,画质基本不受影响,推荐低配用户开启。

---

## 六、常见问题与注意事项

### 6.1 设备与性能

- **任何电脑都能用**:软件启动时自动检测 GPU;无 GPU 或显卡不兼容自动切换 CPU,功能完整、速度较慢;
- **NVIDIA RTX 50 系用户注意**:40 系及更早卡可正常 GPU 加速;50 系(Blackwell)因 Vulkan 驱动缺指令,软件会自动检测并回退 CPU——这是显卡驱动兼容问题,非软件缺陷,请放心使用,输出结果不受影响;
- 显存不足时软件自动分块处理或降低批次;**大图处理失败请先关闭其他占用显存的程序,或勾选「兼容模式」**。

### 6.2 处理质量建议

- 动漫素材推荐 waifu2x / Real-CUGAN;照片素材推荐 Real-ESRGAN / Real-ESRGAN 视频引擎;
- 高清素材通常无需超分,直接「1x 超分」提清晰度即可;
- 高倍率放大(4x)会显著增加噪点,建议保留原图,按需分步操作。

### 6.3 文件与路径

- 输出默认与源文件同目录,可自定义;
- 多图处理会自动建子文件夹,避免文件名冲突;
- 处理中请勿手动删除输出目录文件。

### 6.4 关于 AI 引擎

- 本软件使用开源引擎:waifu2x、Real-ESRGAN、Real-CUGAN、RIFE(以上基于 nihui/ncnn)、U²-Net、ISNet、BiRefNet(基于 ONNX Runtime)、FFmpeg;
- 所有引擎均为本地推理,离线可用,无网络依赖。

### 6.5 反馈与支持

- 使用中遇到问题,欢迎在「💬 ALH Pro 社区」讨论或向作者反馈;
- 若本软件对您有帮助,可在左下角「☕ 请作者喝咖啡」支持作者,表示感谢!

---

*ALH Pro —— 大道至简,本地优先。*
