# ALH Pro 使用说明(v1.0)

开始前先看两处:

1. 左侧导航 = 三个页面:**图片放大 / AI 抠图 / 视频处理**,各管各的;
2. 页面左边一列是设置,右边是结果区。所有参数改完即时生效,先调好再点「开始处理」。

---

## 图片放大

### 你要做什么

把一张图变清晰 / 变大。流程:添加图片 → 选算法 → 选倍数 → 开始处理。

### 第 1 步:添加图片

- 点「添加图片(可多选)」或直接拖入(支持 PNG / JPG / WebP / BMP,支持多张批量);
- 图片列表选中一张后,效果预览、参数都只对这一张显示。

### 第 2 步:选算法(关键)

| 素材类型 | 选哪个 |
|---|---|
| 动漫 / 插画 / 二次元 | **动漫插画 (waifu2x)** |
| 照片 / 实拍 / 人像 | **真实照片 (Real-ESRGAN)** |

选错算法效果会明显变差(动漫用 Real-ESRGAN 会发糊,照片用 waifu2x 会偏画)。

waifu2x 下面还有模型:
- cunet(通用):画质优先;
- upconv_7_anime(动漫插画):速度更快,适合线条简单的漫画。

### 第 3 步:选倍数

- **不放大 (1x)**:只降噪不放大(照片去噪用);
- **1x 超分(2x 放大后缩回)**:只变清晰不放大尺寸,推荐;
- **2x / 3x / 4x**:直接放大。**灰色项 = 当前模型不支持**(引擎决定,不是软件限制)。

### 第 4 步:降噪(可选)

- 降噪级别:不降噪 / 弱 / 中 / 强。**图上有明显的压缩块、噪点时开「中」**,图本身干净就「不降噪」;
- 照片模式有「预处理降噪」:照片噪点多时勾上,强度选弱~中即可,太强线条会糊。

### 第 5 步:高级(可选)

默认不用动。图太灰→「清晰」;图发虚→「去模糊」;线条有锯齿→「边缘抗锯齿」。
**一次只加一项,看效果再叠加,别全开。**

### 结果

- 输出目录默认和源图同目录(可改);
- 多张图会自动建子文件夹,不用管;
- 格式默认 JPG(通用);要透明背景选 PNG;JPG 质量 85~95 即可,再高文件大一圈效果看不出。

---

## AI 抠图

### 你要做什么

去掉背景,只留主体。流程:添加图片 → 选模型 → 微调 → 预览 → 保存到输出目录。

### 第 1 步:添加图片

「输入」下选图,支持多张批量。

### 第 2 步:选模型

| 用途 | 选哪个 |
|---|---|
| 一般没试过,不知道选啥 | **BiRefNet 高精度(默认推荐)** |
| 人像 / 产品,边缘要干净 | ISNet 精细边缘 |
| 动漫插画 | ISNet 动漫 |
| 速度快、批量多 | U²-Net 轻量 |
| 背景复杂、多个主体 | BiRefNet 完整版 |

### 第 3 步:参数(先不动,看效果再调)

- 前景 / 背景阈值:主体与背景颜色接近时,适当降前景阈值、升背景阈值;
- 边缘羽化:数值越大,边缘过渡越柔和(抠毛发、半透明边缘时调);
- 边缘增强:边缘太毛糙时提高;
- 边缘清理:边缘有碎点、杂色时开启。

### 第 4 步:看效果

右侧预览区:处理完直接看;「按住查看原图」对比前后。彩色预览 = 原图 × 蒙版,棋盘格 = 透明区域。

### 结果

保存到输出目录(默认与源图同目录,多张自动分子文件夹)。格式和图片放大共用设置,透明背景记得用 PNG。

---

## 视频处理

### 你要做什么

视频超分(变清晰)/ 补帧(变流畅)/ 去重(删重复帧,动漫片源必备)。流程:添加视频 → 勾选要做的处理 → 设参数 → 开始。

注意:**至少要勾上「超分」或「补帧」其中一项**才能开始处理,只要去重的话也要勾上补帧(去重本身是预处理)。

### 第 1 步:添加视频

拖入或点列表添加,支持 MP4 / MKV / MOV / WebM,多视频批量。每个视频可单独裁剪(右侧时间线拖动),也可单独设置帧率。

### 第 2 步:视频超分(可选)

| 素材 | 引擎 |
|---|---|
| 老动画 / 低清动漫 | 经典动漫 (waifu2x,最快) |
| 真人 / 实拍 | 通用 (Real-ESRGAN) |

倍数:1x 超分 = 只变清晰不变大小(推荐先试);2x 以上放大。**倍率越高越慢、显存占用越大**,笔记本或老显卡用 1x~2x。

### 第 3 步:补帧(RIFE,可选)

**补帧 = 在原有帧之间脑补出中间帧,动作更流畅。** 动漫 24fps 补到 60fps、电视 25/30fps 补到 60fps 都是常见用途。

- 模型:默认「通用画质最新 (RIFE v4.13)」就行;老素材不兼容时再试 v2.3;
- **帧率**:点「视频帧率」展开。默认「各视频默认帧率」= 每个视频按自己原帧率处理,什么都不用设;
  - 想统一定 60fps:选「单独调整各视频帧率」,把批量统一帧率填 60;
  - 想降帧率(如 120→60):选「帧率偏移」,滑块 -60;
- 输出倍率:补帧输出帧率 = 原帧率 × 倍率(默认自动;手填如 60 直接指定)。

### 第 4 步:去重复帧(动漫必备,可选)

**去重 = 删除静止/重复画面(动画里一张画停 3 帧那种),只留真实内容帧。** 用途:让补帧更准、文件更小。

- **智能检测(默认)**:自动估节奏,不用管。策略:均衡(推荐)/ 激进(删得多)/ 保守(几乎不误删);
- **动漫模式**:你知道片源是几个"拍"(一拍二/一拍三)时用,选对应档位;
- **手动模式**:高级用户,默认别碰;
- 勾上「相位自动对齐」,防止抽到中间帧。

注意:去重 + 补帧的最终输出时长 = 源时长(尾帧保留),帧率 = 内容帧率 × 倍率。

### 第 5 步:输出

- 码率:自动(推荐)。不满意再试 中 / 高;「极高」文件很大;
- 格式:MP4(通用,推荐);MKV 老播放器可能不认;
- 编码:H.264(通用)做默认;H.265 同画质文件小一半,但老设备可能播不了;
- 静音:只要画面时勾上(比如补帧素材,反正源没声音)。

### 兼容模式

勾上后自动降低显存占用,防爆显存/卡死,画质基本不变。**没独立显卡、显存小、或不稳定时勾上**,别的场景不用开。

---

## 常见问题

### 处理很慢?

- CPU 模式比 GPU 模式慢几倍到十几倍,正常;
- 用高倍率、大分辨率的素材本来就慢,降倍率试试;
- 底部状态栏有进度和已用时间,可看还有多久。

### 这台电脑能用 GPU 加速吗?

软件启动时自动检测:有独立显卡且兼容 → 自动启用;否则自动切 CPU,不影响功能只是慢。**RTX 50 系(5090/5080 等)目前已知不兼容 GPU 加速,软件会检测到并自动用 CPU**,这是显卡驱动问题,输出结果不受影响。

### 引擎缺失 / 报错?

状态栏显示「引擎缺失」时,检查软件目录下 engines 文件夹是否完整。处理过程报错先看诊断日志(设置 → 诊断日志)。

### 结果不是预期?

- 动漫图发糊 → 检查算法是不是选了 Real-ESRGAN(应该用 waifu2x);
- 抠图边缘粗糙 → 调高边缘增强 / 换 ISNet;
- 视频补帧有果冻 → 勾「果冻修复」/调光流时间步;
- 输出没声音 → 检查「静音」是否被勾上了。

---

## 说明

- 软件所有处理在本机完成,文件不会上传;
- 引擎与模型版权归属见软件内「关于」→ 完整许可声明;
- 使用中遇到问题,去「💬 ALH Pro 社区」反馈,或联系作者。
