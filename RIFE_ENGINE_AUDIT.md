# RIFE 补帧引擎:重编 + 探测加强 审计(2026-09-12)

> 本文件记录**做过什么、实测到什么、没验证什么**。所有数字都是本机实测(RTX 4060 Laptop / i7-12650H /
> 驱动 572.83 / 7680×4320 显示器),脚本在 `_qa/rifeprobe/`、验收工具在 `_qa/rifeprobe_app/`。
> 规矩:**没实测过的不写"可用"**;对比图放桌面。

---

## 0. 结论摘要(一句话版)

1. **RIFE 引擎重编完成**:改用与 Real-ESRGAN 重编同一套 2025/2026 ncnn 源码,静态指纹与那份已实测可用的
   引擎完全一致(旧引擎是 0 命中)。**数值等价**(实测逐帧 PSNR 76.8dB / 82.1dB)、**略快**,程序优先用它。
2. **补帧 GPU 探测加强**:探测尺寸改为**源分辨率**、探测画面换成**有确定预期**的渐变对、判据从 1 条加到 4 条、
   结论缓存键带上**引擎指纹 + 分辨率档**。真机(已部署的出厂代码)实测:**1080p 1.5s / 4K 2.2s 通过**。
3. **过程中挖出两个真问题**(都是"工具说成功、结果其实坏"的那种):
   - 重编必须改上游源代码两处"只在 MSVC 下能过"的写法,否则**退出码 0、输出全黑帧**;
   - 引擎前端对**带 alpha 的 PNG** 会读错通道 → 输出乱码帧(探测图必须与生产同为 24bpp/JPG)。

---

## 1. 为什么要重编(静态指纹证据)

判定方法:直接在 exe 字节里数 Vulkan 扩展名出现次数(脚本 `_qa/fingerprint.py`)。
2025+ 的 ncnn 会引用 robustness2 / cooperative_matrix 这套兼容处理;2022 年的 ncnn 一次都不提。

| 引擎 | 体积 | `VK_EXT_robustness2` | `VK_KHR_robustness2` | `VK_KHR_cooperative_matrix` |
|---|---|---|---|---|
| **RIFE 2026 重编(新)** | 36,399,134 | **2** | **2** | **364** |
| RIFE(旧,2026 之前下游给的) | 7,242,752 | 0 | 0 | 0 |
| RIFE(旧,2022 备份) | 6,974,464 | 0 | 0 | 0 |
| Real-ESRGAN 2026 重编(已知可用) | 32,077,832 | 2 | 2 | 339 |
| Real-ESRGAN(2022 旧) | 6,161,408 | 0 | 0 | 0 |
| waifu2x 20250915 | 5,094,912 | 1 | 1 | 105 |

新 RIFE 与"已知可用"的 Real-ESRGAN 2026 重编**指纹一致**;旧 RIFE 两项全 0。

## 2. 重编踩到并修掉的坑(每条都在源码注释里写清了原因)

| # | 症状 | 根因 | 修法 |
|---|---|---|---|
| 1 | `glslangValidator -V -s -x` 失败:shader 第 20 行 `syntax error, unexpected IDENTIFIER` | RIFE 的着色器用了 ncnn 的宏类型 `sfp`,该类型由 ncnn **在线编译**时注入;realesrgan 的 shader 自己在文件里定义了 `sfp`,所以那条路对它通、对 RIFE 不通 | 改用上游那套"GLSL 源码嵌成 hex、运行时在线编译"(`generate_shader_comp_header.cmake`) |
| 2 | 链接期 `undefined reference to Warp::Warp()` | `Warp` 的实现不在 rife.cpp,在上游的 **warp.cpp**;还漏了 3 个 warp 着色器 | 补 `warp.cpp` + `warp.comp/warp_pack4.comp`(清单照抄上游 CMakeLists 全表) |
| 3 | 编译 `warp.cpp` 报 `Option has no member named 'use_shader_pack8'` | 现代 ncnn **已彻底移除 pack8**(在 ncnn-master 里 grep `use_shader_pack8` 零命中,VkMat 只有 pack1/pack4) | 删掉 pack8 的创建分支与 `elempack==8` 分支(现代 ncnn 永远不会走到,纯死代码) |
| 4 | **引擎退出码 0、却输出全黑帧**;stderr 里 `_wfopen r/f.param failed`、`network graph not ready` | 上游 `rife.cpp` 写的是 `swprintf(L"%s/%s.param", modeldir, name)`:MSVC 把宽格式串里的 `%s` 当**宽**串,MinGW 按 C 标准当**窄**串 → `L"rife-v4.6"` 只读出 `r`、`L"flownet"` 只读出 `f`,路径成了 `r/f.param`,三个网络全没加载 | 改成 C 标准的 `%ls`(MSVC 也认,可移植) |

> 第 4 条是最危险的一类:不报错、退出码 0、还写出一张合法尺寸的 PNG —— 只是**全黑**。
> 这一条是靠"真跑一遍并检查输出像素"抓到的,**不是**靠编译通过或退出码。

## 3. 数值等价:新引擎 vs 旧引擎

模型 `rife-v4.6`,同一对输入,两边各跑一遍再逐帧比对。

| 场景 | 尺寸 | 新 vs 旧 PSNR | 新耗时 | 旧耗时 |
|---|---|---|---|---|
| 真实帧单对插值 | 1920×1080 | **76.88 dB** | 3.03s | 3.41s |
| 真实帧单对插值 | 3840×2160 | **82.10 dB** | 3.89s | 4.13s |
| **目录模式(生产路径)** 6 帧 → 12 帧 | 1920×1080 | 逐帧最差 **76.77 dB** | 2.77s | 3.06s |

- 76 dB 量级 = 逐像素最大差约 0.04/255;**均值与方差逐帧一致**(如 200.99/3212.57 vs 200.99/3212.57)。
- 目录模式产出帧数两边一致(都是 12 帧),程序取 `frame_4i-2` 的规则不受影响。
- `-s` 任意时间步两边也一致(0.25 → 2.9,0.5 → 74.4,0.75 → 242.2)。

### 3b. 端到端(整条流水线)实测

用反射调**已部署的出厂代码** `VideoService.ProcessVideoAsync`(68 个参数,只指定"补帧 2x、不超分",
其余一律用它自己的默认值),输入 1920×1080 / 12fps / 1 秒 / 12 帧的短片:

```
调用 ProcessVideoAsync(68 个参数;补帧 rife-v4.6,2x,不超分)
完成 ✅ 20.5s   输出文件: 224,516 字节
产物: h264 / 1920×1080 / nb_frames=23 / r_frame_rate=23/1 / duration=1.000000   ← 12 帧 ×2 正好 23 帧
抽 6 帧坏帧自检:均值 195.14、方差 3975、近黑 1.4% → 坏帧 0 个
```

即:**拆帧 → RIFE(新引擎)→ 去重 → 合帧**这条完整链路能出片,帧率/帧数/时长都对,没有黑帧。
(命令:`RifeAppProbe.exe <发布版目录> video <输入> <输出> rife-v4.6`)

## 4. 探测为什么要换输入:黑→白硬切是病态输入

旧探测输入 = 纯黑帧 → 纯白帧。实测(新老引擎一致):

| 尺寸 | 黑→白硬切插出来的"中间帧"均值 |
|---|---|
| 320×240 | **2.4**(近黑!) |
| 1280×720 | 60.5 |
| 1920×1080 | 74.4 |
| 3840×2160 | 90.1 |

对光流来说这是"没有真实运动、只有内容突变"的**病态输入**,正确结果没有定义 —— 所以旧探测只能查
"三通道是否均衡",**出黑帧/出糊帧一概查不出来**。

新探测输入 = 水平渐变 + 同一渐变右移 2 像素(=真实的小位移),正确结果有确定预期:均值≈两输入均值、方差≈输入方差。

**13 个模型 × 新老两代引擎全部实测**(脚本 `_qa/rifeprobe/sweep_models.py`):

| 判据 | 实测范围 | 判据门槛 | 余量 |
|---|---|---|---|
| 输出均值 − 预期均值 | ≤ 0.24 | 24 | 100 倍 |
| 输出方差 / 输入方差 | 1.00 | ≥ 0.25 | 4 倍 |
| 近黑像素比例 | 3.1~3.3%(渐变暗端本来就有) | < 95% | 29 倍 |
| 三通道极差 | ≤ 0.09 | ≤ 12 | 130 倍 |

覆盖模型:`rife` `rife-anime` `rife-HD` `rife-UHD` `rife-v2` `rife-v2.3` `rife-v2.4` `rife-v3.0` `rife-v3.1`
`rife-v4` `rife-v4.13` `rife-v4.26` `rife-v4.6`。

### 4b. 另一个坑:带 alpha 的 PNG 会被引擎读错通道

同一对渐变图,只改像素格式(脚本 `_qa/rifeprobe/pair_variants.py`):

| 输入格式 | 输出均值 | 输出方差 | 输出横剖面(12 等分点) |
|---|---|---|---|
| 24bpp RGB | 126.70(输入 126.87) | 5414.8 | 0 21 42 63 84 105 127 148 169 190 212 233(仍是渐变) |
| 32bpp ARGB | **158.96** | **2123.4** | 9 159 186 55 64 206 220 230 132 179 202 175(**乱码**) |

- 这不是引擎故障,是"喂了它不认的像素格式"。**生产不受影响**(拆帧就是 JPG、超分产出是 24bpp PNG),
  但 `new Bitmap(w,h)` 默认就是 32bppArgb —— 探测图必须**显式**用 `Format24bppRgb`。
- 第一次实现就踩了这个,真机探测把可用引擎误判成"坏帧",日志里是"亮度不对(均值 158.7,正确结果应≈129.2)"。
  靠"读应用日志拿到具体是哪条判据不过"定位,再靠变量隔离(A/B 两个变体)锁定是 alpha 而不是位移方向。

## 5. 探测改造后的真机验收(跑的是【已部署的出厂代码】)

工具 `_qa/rifeprobe_app/`(反射调 `发布版\ALHPro.dll`,不复制逻辑):

```
VideoService.RifePath = D:\deep\alh-pro\engines\rife\rife-ncnn-vulkan-2026.exe   (36,399,134 字节)
键(360p)  = rife:rife-ncnn-vulkan-2026.exe/36399134:sd:rife-v4.6
键(1080p) = rife:rife-ncnn-vulkan-2026.exe/36399134:fhd:rife-v4.6
键(4K)    = rife:rife-ncnn-vulkan-2026.exe/36399134:uhd:rife-v4.6
键(旧引擎/1080p) = rife:rife-ncnn-vulkan.exe/7242752:fhd:rife-v4.6        ← 换引擎必然换键
1080p(1920×1080) 探测: 可用 ✅  1.5s
4K(3840×2160)   探测: 可用 ✅  2.2s
320x240         探测: 可用 ✅  1.3s
EnsureRifeNcnnProbeAsync 第1次(1080p): 可用 ✅  1.6s
EnsureRifeNcnnProbeAsync 第2次(1080p): 可用 ✅  0.0s      ← 命中缓存
```

落盘结论:`rife:rife-ncnn-vulkan-2026.exe/36399134:fhd:rife-v4.6|0  True  ... rife probe ok; model=rife-v4.6`

> ⚠ 一个容易误读的点:`EngineService.EnginesDir` 优先用"程序自己旁边"的 `engines\`,找不到才向上逐级搜。
> 本工具的可执行文件在 `_qa\rifeprobe_app\bin\...` 下(旁边没有 `engines\`),所以它向上找到的是
> **仓库的** `D:\deep\alh-pro\engines`。也就是说本工具验证的是"出厂代码 + 仓库里的引擎",
> 而不是"安装到别处的那一份" —— 安装版那一份另用安装探针单独验(见第 5b 节)。

## 5b. 安装探针:包里到底有没有新引擎(端到端打包验证)

```
静默安装(不勾"下载模型包",不自动启动):ALHPro_v1.3.4_本体_20260912-2017.exe /VERYSILENT
   /SUPPRESSMSGBOXES /NORESTART /DIR="D:\deep\_install_probe_rife"
安装退出码 0,用时 72 秒
{app}\engines\rife\rife-ncnn-vulkan-2026.exe   36,399,134 字节
  SHA256 = ABBE281E8F90D868F80028847085740A7FFDC7E5B621A8CBF2CA70388425DA02  ← 与构建产物**完全一致**
{app}\engines\ffmpeg8                          9 个文件 / 169.6 MB
用【安装后的】引擎 + 安装后的模型实跑一对:exit=0、正常出帧
```

即:安装包里确实带上了新引擎,且带的是**同一个二进制**(哈希一致)。验完已卸载探针目录。

## 6. 超时定标(实测,不是拍的)

| 场景 | 实测 |
|---|---|
| 320×240 冷启动(首次运行) | 8.3s |
| 320×240 热 | 1.5s |
| 1080p | 1.4~3.0s(最重模型 `rife-anime` 3.5s) |
| 4K | 2.2~5.1s(最重模型 7.2s,旧引擎) |

故取 **<1080p 15 秒 / ≥1080p 25 秒**(对最坏实测 3 倍余量),仍远小于 8 分钟看门狗。

## 7. 改动清单

| 文件 | 改动 |
|---|---|
| `AlhPro.Core/DeviceRouting.cs` | 新增 `InterpProbeMeanTolerance(24)` / `InterpProbeStructureRatio(0.25)` / `IsPlausibleInterpOf` |
| `AlhPro.Core/ProbeDiagnosis.cs` | 坏帧形态文案覆盖"黑帧/亮度/结构"(不再只说近黑),仍**不提驱动/NVIDIA** |
| `ImgUpscalerUI/EngineService.cs` | 探测输入换成 24bpp 渐变对;`ProbeOutputIsSane` 四判据;`SamplePngStats` 采样统计;超时按面积;`RifeProbeKey` 带引擎指纹+尺寸档 |
| `ImgUpscalerUI/VideoService.cs` | `RifePath` 优先 2026 重编版(缺则回退旧版);探测按源分辨率调用 |
| `打包.ps1` | 必带清单加 `engines\rife\rife-ncnn-vulkan-2026.exe` + 自动补拷 |
| `AlhPro.Tests/DeviceRoutingTests.cs` | 新增 13 条判据单测(275 → **288 全绿**) |
| `RELEASE_NOTES.md` | v1.3.4 新增第 29、30 条 |

## 8. 没验证的部分(不许当"已验证")

- **AMD / Intel / RTX 50 系真机**:本机只有 RTX 4060 Laptop(Ada),上述结论只在这台机器上成立。
- **驱动 ≥610 的行为**:本机驱动 572.83,没测过更新驱动后的表现。
- **主观观感**:本 AI 读不了图,所有"等价"都是逐像素 PSNR/均值/方差口径;**没有做人眼观感评估**。
- **4K 大模型冷启动的组合**(4K × `rife-anime` × 首次运行)未单独实测,超时余量按最坏单项估计。
- **端到端只跑了这一种配置**:1080p / 12fps / 1 秒 / 12 帧 / 补帧 2x / 不超分 / 去重关闭。
  没跑过:4K 长片、超分+补帧同开、其它补帧倍率(3x/4x/8x/12x/16x)、TTA(-x)、12 个其它补帧模型、
  音频/字幕流、断点续传、取消/停止。
- 帧率从 12fps 变为 23/1(约 23fps)—— 这是"12 帧补成 23 帧"的正常结果(2x 的严格算法是 2n−1),
  但**不是** 24fps;与旧引擎行为一致(旧引擎目录模式同样产 12 帧 → 程序取 4i−2 那批),
  本轮没有改动这部分逻辑。
