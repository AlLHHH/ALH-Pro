<?xml version="1.0" encoding="UTF-8" ?>
<Window
    x:Class="ALHPro.Views.UpdateLogWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource AppBgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Grid Grid.Row="0" Padding="18,14,10,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="更新日志 · ALH Pro" FontSize="17" FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
            <Button x:Name="CloseBtn" Grid.Column="1" Content="✕" FontSize="12"
                    Padding="10,6" Background="Transparent" BorderThickness="0"
                    Click="Close_Click"/>
        </Grid>

        <!-- 作者的话(卡片,最顶部) -->
        <Border Grid.Row="1" Margin="18,0,18,12" CornerRadius="8" Padding="14,10"
                Background="{StaticResource AppPanelBrush}">
            <TextBlock x:Name="AuthorText" FontSize="12.5" Opacity="0.88"
                       TextWrapping="Wrap"/>
        </Border>

        <!-- 版本列表 + 内容 -->
        <Grid Grid.Row="2" Margin="18,0,18,0" ColumnSpacing="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ListView x:Name="VersionList" Grid.Column="0"
                      SelectionMode="Single"
                      Background="{StaticResource AppPanelBrush}"
                      BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1"
                      CornerRadius="8" Padding="6"/>
            <Border Grid.Column="1" Background="{StaticResource AppPanelBrush}"
                    BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1"
                    CornerRadius="8" Padding="12">
                <ScrollViewer x:Name="ContentScroll" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="ContentText" FontSize="13"
                               TextWrapping="Wrap"/>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- 底部按钮(升级模式:开始使用;手动模式:关闭) -->
        <Grid Grid.Row="3" Padding="18,14,18,16">
            <Button x:Name="PrimaryBtn" Content="开始使用" HorizontalAlignment="Right"
                    MinWidth="160" Padding="18,8"
                    Style="{StaticResource AccentButtonStyle}" Click="Primary_Click"
                    Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Window>
