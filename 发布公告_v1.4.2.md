**v1.4.2:四批实测修复 + 视频抠图整条下线 + 视频 2x 实测提速 28 倍 + 音频 / 显卡「白试」落盘**

<sub>2026-09-24</sub>

> **这一版主要做什么**:把四批实测修复合在一个版本里 —— ① 一用就撞上的硬故障(Anime4K 在走备用 ffmpeg 的机器上必然失败、「一键参考预估」从来没成功过、开机默认页对不上、音频页三个按钮永远灰、「处理完成」弹窗撞「预览效果」会崩、显存降档重试是空转);
> ② 一批"不报错、只是慢或者悄悄做错"的老毛病(音频"显卡不行"记不住账、「自定义分辨率」入口丢了、补帧可能悄悄少帧、视频可能静默走 CPU 软编);
> ③ **视频抠图整条下线**(你定案"以后不会再做了");④ 视频 2x 被静默推到"慢 13 倍"的那条路已修,**实测同一段素材快 28 倍**。
>
> **升级方式**:直接覆盖安装,**不需要先卸载**;设置 / 预设 / 记录都在 `%LOCALAPPDATA%`,不用重下模型。
> ⚠ **两处需要留意**:① **「视频抠图」页面整条删除**(它从建好起就一直锁着"开发中",从未开放过;图片抠图与其它页面不受影响);② 「自定义分辨率」入口**补回来了**(倍率现在是 1x 修复 / 2x / 3x / 4x / 自定义分辨率)。

---

### 一、一用就撞上的硬故障

- **Anime4K 在"合帧走备用 ffmpeg"的机器上必然失败 —— 已修**:「1x 修复 · 动漫 Anime4K」的滤镜挂在**合帧**那一步,而一部分机器(典型是驱动较旧的 N 卡)硬件编码探测不过时会自动改用随包的**备用 ffmpeg8** —— 那个目录以前**没有 `shaders` 子目录**,滤镜直接报错、整条任务失败。**实测**:同一条命令在主 ffmpeg 目录下退出码 **0**、在 ffmpeg8 目录下退出码 **-2**;现在两个目录都带着色器,ffmpeg8 也返回 **0**。这类失败以前还被报成"硬件编码(nvenc)连续 3 次失败"(让人去折腾显卡驱动),现在会明说是**滤镜链**的问题。
- **「一键参考预估」(内容帧率 / 可删比例)从来没成功过 —— 已修**:采样命令里的 `-vsync 0` 在当前随包 ffmpeg 上**已被移除**,必然报 `Unrecognized option 'vsync'` ⇒ 每次都走降级分支,只挂一个「无法预估」角标。改成 `-fps_mode passthrough` 后**实测退出码 0**,采样产物大小正好(90 帧 × 2304 字节 = **207,360 B**)。
- **开机默认页对齐(设置里选哪个就是哪个)**:启动页编号与左侧导航的书写顺序**不是同一套**,旧代码把前者直接当后者用。**实测**:设置里选「视频处理」→ 重启进的是当时还锁着的「视频抠图」;选「视频抠图」→ **完全不生效**;「上次退出界面」也有两个错。换算收进带单测的 `AlhPro.Core.StartupPageMap`,功能页两两互不撞车。
- **音频处理页的「删除选中 / 清空 / 清除已完成」永远灰 —— 已修**:这三个按钮以前只在"删除 / 清空 / 框选"之后才刷新(实测同一时刻视频页与图片页是亮的、只有音频页灰)。现在"加完文件"与"选中变化"都会刷新。
- **「处理完成」还开着时点「预览效果」会抛未处理异常(实测崩溃)**:WinUI 同一时刻只允许一个对话框,第二个弹不出来会直接抛 `0x80000019`,用户只看到"点了没反应"。现在降级成状态栏那句话 + 一条日志,不再抛到顶层;被挡回时的提示也改成实话(以前让人去看日志,日志里其实一个字都没有)。
- **显存不足时的"自动降低分块重试"以前是空转 —— 已修**:日志与进度条都在说"分块 auto→512→256→128 逐级降低",但那两条引擎命令的分块参数是**写死的 `-t 0`** ⇒ 同一参数白跑 3 次才失败。现在真的按 auto→512→256→128 走。
- **三处悬停提示说错了(照做会丢任务)**:三页红色的「强制结束」原写着"休息时变「跳过休息」,点一下跳过休息继续处理" —— 实际这个按钮**从来不会变文案**,照提示点下去就是**中止整个任务**。文案已改成实话,并指明真正的「跳过休息」在窗口底部状态栏右侧。
- **文案纠偏**:"视频批基准 1400 帧/批(实际每批 50~400,按素材长度定)"这句自相矛盾(真实区间是 **80~1400**),三处已改。

### 二、平时看不见、但一直存在的老毛病

- **音频分离"显卡不行"这件事记不住账 —— 已修**:AI 分离靠一张"这台卡用不了了"的连击表避免反复白试,但**记账用的编号错了** —— 那张表只认**显卡(DML)编号**,代码却拿**引擎编号**去记,这两个数在双显卡 / 带核显的机器上不是同一个 ⇒ 账记到别人头上,连击永远攒不够,每首都要再白试一遍(实测每次 **2~11 秒**)。现在统一成同一套编号,并且"建显卡会话当场失败"就直接判这张卡在音频上不可用(重启软件会重新试一次)。
- **「自定义分辨率」其实是丢了的 —— 已还原**:那个面板(填宽 × 高,超分后精确缩放到你要的尺寸)只在倍率选到**第 5 项**时才显示,而倍率下拉里**只有 4 项** ⇒ 面板永远隐藏,使用教程那句"选「自定义分辨率」精确指定输出宽高"照着做也找不到。现在倍率是 **1x 修复 / 2x / 3x / 4x / 自定义分辨率**;**顺带修掉一处连带 bug(自查时抓到)**:设置里存的是倍率索引,旧规则把索引 4 一律当成 2x ⇒ "存得住、读不回"(重启会被静默打回 2x,设置文件里那个 4 还会被**改写成 1**);实测现在 960×540 存得住也读得回。
- **补帧交付失败会"悄悄少帧" —— 现在会留痕**:搬运两次都失败时旧代码照样把帧号加一 ⇒ 帧序列里出现空洞(下游按 `frame_%06d.jpg` 读,要么报错、要么少帧静默变短),日志里一个字都没有。现在只有**真的搬成功**才推进帧号(序列始终连续),失败前多给两次机会,失败一定写日志(逐帧 WARN + 段末 ERROR 汇总,并说明本段少了几帧)。
- **"视频不落 CPU"这条策略的最后一处口子 —— 堵上了**:旧版报错会建议你"到设置里选「CPU 计算」",但那个选项**早就不在设置里**;更麻烦的是代码把"设备编号 < 0"当成了"用户选了 CPU",而 **Vulkan 自检失败**时软件会临时把编号置 -1 ⇒ 这种机器上的视频会**静默**走 CPU 软编。现在视频一律要求可用硬件编码器,没有就**开跑前直接报错**并给出可执行的自救(更新 / 重装驱动、重启、确认显卡没被禁用)。**不受影响**:图片放大 / 抠图 / 音频处理没有显卡时照样用 CPU 跑完。
- **四处"说法过时" —— 已改成实话**:降噪开关的提示还在教人"选「自动」时会先给素材体检"(那个档位已下线);图片抠图的模型说明写着"本机 GPU 实测最快(0.4 秒/张)",而图片抠图**固定用 CPU**;音频页一开工就写"显卡加速",可它只是在**尝试**显卡(建会话失败就自动改 CPU);批大小报错正文里写死了过时数字。使用教程与官网的同类说法也一起改了。
- **清掉一批"死东西" + 更新检查不再刷屏**:三处永远看不到的界面与四处零调用点的死代码全删;本机 GitHub 接口稳定 403 / 超时时一次启动会刷 6 条几乎一样的日志 ⇒ 现在同一通道只详细报一次,末尾给一句汇总;音频列表处理完的文字("文件名 (已处理)")以前不会出现在界面上,现在会刷新。
- **这一版「故意没做」的一件事(说明白)**:核查报告里的「分段编码自动触发」**没做** —— 分段编码目前只作用于**编码**阶段,而峰值磁盘占用发生在更早的"逐帧处理"阶段,中途自动开分段只会让"本来要失败的任务"多跑一段再失败。维持现有口径:**开跑前按实际峰值算够不够,不够就拦下并给量化建议**,不假装能自动兜住。

### 三、视频抠图 —— 整条删除

- 那一页 2026-09-22 建好后**一直锁着"开发中 · 敬请期待"**、从未开放过;它的成本与收益不匹配:1080p 逐帧(ONNX 推理 + 蒙版后处理 + 时域稳定 + 合帧编码)实测约 **0.35 秒/帧** ⇒ 60 秒的片子约 **10.5 分钟**,而"换背景 / 出透明通道"在剪辑软件里是常规操作。所以整条删掉:页面、导航项、启动页下拉项、服务层,以及只为它存在的死代码。
- **按你说的"留一点有用的"保留了两样**:① `AlhPro.Core` 里那批纯逻辑工具件(蒙版后处理阈值 / 羽化 / 形态学、换背景合成、镜头切点判定 + 时域稳定滤波,都带单测,将来做图片抠图的边缘质量或任何逐帧蒙版能直接拿来用 —— 实测 1080p 后处理默认档 82 ms/帧、时域滤波 3.9 ms/帧、合成 10.3 ms/帧);② 一份新文档,把四个抠图模型的 GPU / CPU 速度与"软边占比"、"只出蒙版 vs 完整路径"相差 120~134 ms/帧、透明视频编码的两个坑都留在仓库里 —— 都是花钱测出来的。
- **老设置兼容**:启动页若存过"视频抠图"会被判为非法 ⇒ 自动回落到"上次退出界面"。左侧导航现在剩 **4 个功能页**(图片放大 / 图片抠图 / 视频处理 / 音频处理),图片抠图(固定 CPU)与其它页面完全不受影响。

### 四、视频 2x 被静默推到"慢 13 倍"的路 —— 已修

- **现象**:同一台机器、同一段 3 秒 720p 素材,「2x 超分」实测 **3880 毫秒/帧**(90 帧要跑约 6 分钟),而软件自己给这支模型标的是 **0.26~0.30 秒/帧**。
- **根因是一串很隐蔽的连锁**:视频页「动漫 · Anime4K 修复(快)」的内部标识 `anime4k` 实际走 ffmpeg 着色器、**根本不用 ncnn 引擎**,但开跑前的"显卡兼容性预检"会把**当前选中的那一项原样**当模型名交给 ncnn 去加载 ⇒ ncnn 被要求加载一个不存在的模型 ⇒ 探 60 秒没响应被强杀 ⇒ **落一条假的"realesrgan 在本机不可用"结论**;而"独显 + 核显"的机器每次预检都带模型名、从不写"引擎级"结论 ⇒ 那条假失败就成了整条 Real-ESRGAN 引擎的判据 ⇒ 之后**所有** 2x/3x/4x 视频超分都改走 ONNX 慢路。
- **修法**:① 预检前把"伪模型"回落成"探引擎默认模型",不再落假结论;② 兜底的引擎判定只统计**真 ncnn 模型**的结论 —— 伪模型不参与(已经落盘的旧假结论也会因此被忽略,**不用你自己删文件**)。
- **实测(修后、同一段素材同一台机器)**:超分路线回到 **ncnn-Vulkan**、**136 毫秒/帧**,整个任务 **22.3 秒**(修前同配置约 6 分钟)⇒ 单帧快 **28 倍**;日志还带上 GPU 利用率采样(均 56%)作旁证。

### 五、结论不再反复"白试",启动自检也不再晾人

- **显卡探测:一次"60 秒无响应"不再把整台机器判成慢路**(那条慢路实测 3880 毫秒/帧,快路只要 ~135 毫秒/帧)。只有"无响应 / 超时"这种**瞬时**形态会先退避 3 秒重试一次,仍失败才落盘;确定性失败(初始化即崩、参数错误、出图但坏帧)一次都不重试;日志如实写"第几次失败 → 退避后重试 → 结果"。
- **同时修掉两条独立审查抓到的**:① 用户在探测卡住时点「取消」,以前会**落一条 1 天的失败结论**(等于因为一次取消、当天全走慢路)且日志错误归因成"确定性失败" —— 现在取消一律**不落任何结论**;② 日志里"重试第 2/1 次"这种自相矛盾的措辞已修。
- **音频那条"白试"跨天也记得住了**:失败结论与成功一样记 **7 天**(第 1、2、3 次会话累计到上限后,第 4 次起就不再白试);并新增**环境签名**(账本里记一行"显卡名@驱动版本",装 / 换驱动、换卡、插拔 eGPU 时**整份结论作废、重新试一次**,不用你自己删文件)。
- **启动自检的"所有候选卡都卡死"最坏耗时砍掉三分之一**:挑卡档对"卡死"原也重试 3 次,而每次卡死都要等满 15 秒超时、还要在多张候选卡上逐个试 ⇒ 独立审查算过最坏约 **4.8 分钟**;现在"卡死"单独限 2 次、最坏约 **3.2 分钟**("进程崩一下"这类确定性失败仍是 3 次保险不变)。

### 六、AMD / Intel 机器现在也能真读空闲显存了

- 此前"当前空闲显存"只有 NVIDIA 能真读(nvidia-smi),A 卡与核显上一律报"未实测" ⇒ 显存墙与并发档位在那些机器上整层失效。现在补了第二条**跨厂商**路径:用 Windows 自带的性能计数器「GPU Adapter Memory」读"已用",再与 DXGI 报的显存总量相减得空闲(换算与实例名解析都在 Core、有单测);日志与诊断包里会写明这个数字**是谁给的**(`6.3 GB(nvidia-smi)` / `6.5 GB(PDH(GPU Adapter Memory))`),排查时不用再猜。
- **边界(如实)**:多卡机拿不到目标卡 LUID 时按全部实例求和 ⇒ 偏保守(宁可少放批次);两者都读不到时仍照旧报"未实测",**绝不返回估值冒充实测**。AMD / Intel 的**温度**读数本版没做:Intel 侧没有公开接口、AMD 需 ADL 且本机无 A 卡,不发没验过的。

---

### 下载

| 文件 | 大小 | 说明 |
|---|---|---|
| `ALHPro_v1.4.2_Setup.exe` | **678 MB**(711,249,499 字节) | 标准版安装包,**不含** AI 抠图模型包 |
| `models_v1.0.zip` | 约 **1.4 GB**(1,479,029,997 字节) | AI 抠图模型包,解压到 `engines\rembg\` |

嫌分开装麻烦,可以用**网盘完整版**(约 2.0 GB,含全部模型)—— 链接与提取码见官网下载页。

> 完整更新日志见仓库 `RELEASE_NOTES.md`;免责声明 / 打赏 / 版权许可见 `声明.md`;第三方开源许可见 `THIRD_PARTY_NOTICES.txt`。
